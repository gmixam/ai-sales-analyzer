# Progress Log — AI Sales Analyzer

## Текущий статус
**Этап:** Веха 6.5 — Business-ready Report Pack
**Статус фазы:** Веха 6.5 — Step 8K: OnlinePBX expired recording URL recovery audit completed (2026-05-05)
**Дата начала:** 2026-03-17
**Последнее обновление:** 2026-05-05

## Что сделано
- [x] 2026-05-05 — Manager_daily Step 8K: source audio availability / OnlinePBX 404 recovery audit выполнен без изменения Step 8I gate, analyzer prompts, STT/LLM logic, scoring, eligibility, selection model, rolling window, renderer semantics, delivery semantics, `rop_weekly` или scheduler. **Root cause:** оставшиеся 9 `Без транскрипта` calls Эльмиры/Тимура имеют persisted `raw_ref`, но это direct OnlinePBX `api2.onlinepbx.ru/calls-records/download/.../rec.mp3` URL с истёкшим ключом; old URL probe по всем 9 вернул HTTP `404`, `content_type=application/json`, body `{"message":"KEY_IS_EXPIRED","error_code":"KEY_IS_EXPIRED"}`. Это не quota, не STT/LLM, не auth/header loss, не source retention loss и не missing canonical id. **Canonical recovery evidence:** для всех 9 rows есть OnlinePBX canonical uuid в `interactions.external_id` и `metadata.external_call_code`; `get_cdr_list("2026-05-04")` нашёл все 9 records по uuid (`cdr_total=311`), хотя CDR record itself не содержит `record_url`; `OnlinePBXIntake.get_recording_url(uuid)` успешно re-resolve’ит fresh URL для всех 9. One-call safe probe на `98aa9872-5406-4eef-9c0b-30f44175b3a9` подтвердил fresh URL `200 audio/mpeg`, full audio-only extractor download `368208` bytes, temp file deleted, no STT/LLM/DB mutation. **Code-path finding:** persisted `raw_ref` stores direct expiring URL, while canonical uuid is preserved separately; `_prepare_artifacts()` downloads `interaction.raw_ref` as-is and does not refresh expired URLs. Full report-run source discovery can resolve recording URLs for targeted records before save, but exact-ID controlled build path bypassed that prelude. **Recommendation:** implement bounded `source_audio_url_refresh` before transcript build: if source is OnlinePBX and canonical uuid exists, refresh `raw_ref` via `get_recording_url(uuid)` when `raw_ref` is missing/expired or just before audio fetch in controlled recovery mode; then process at most 1-2 calls first. If refresh lookup fails, persist/return operator-facing `source_audio_unavailable` diagnostic (not manager-facing normal outcome) and keep Step 8I gate blocking until operator review. **Docs:** implementation follow-up added to `BUSINESS_READY_REPORT_PACK_TASKS.md`; `DECISIONS.md` not updated because no standing behavior was implemented or approved yet.
- [x] 2026-05-05 — Manager_daily Step 8J: controlled completeness closure build выполнен малыми exact-ID batch без изменения кода, analyzer prompts, STT/LLM logic, scoring, eligibility, selection model, rolling window, renderer semantics, `rop_weekly`, scheduler или delivery semantics. Использован internal CLI path в `api` container через `CallsManualReportingOrchestrator._prepare_artifacts(..., mode="build_missing_and_report")` по точным `interaction_id`; report rendering/delivery path не вызывался, business email и Telegram не отправлялись. **Before:** Эльмира `meaningful=12`, normal `3`, `Без транскрипта=4`, `Не подходит для разбора=5`, gate `review_required`; Тимур `meaningful=15`, normal `6`, `Без транскрипта=5`, `Не подходит для разбора=4`, gate `review_required`; Толеген `meaningful=6`, normal `4`, `Без анализа=1`, `Не подходит для разбора=1`, gate `review_required`. **Batches:** Эльмира E1/E2 exact report-day blocking calls `2+2` selected, Тимур T1/T2 `3+2` selected, Толеген TO1 `1` selected. For Эльмира/Тимур all 9 no-transcript calls had `raw_ref`, but transcript build failed before STT because OnlinePBX audio download returned `404` (`Failed to download audio artifact: status=404 ... rec.mp3`); `transcripts_built=0`, `analyses_built=0`, `skipped_due_to_quota=0`, `quota_blocker=null`, no delivery invoked. Толеген call `2e7cbe47-9853-40ee-b36e-995134be6d8f` reused transcript, built reusable analysis `30aac88c-b269-4b2b-953d-a07da8830d1d`, final status `tech_service`; manager gate changed to `passed`. **After:** Эльмира remains `Без транскрипта=4`, gate `review_required`; Тимур remains `Без транскрипта=5`, gate `review_required`; Толеген `Без анализа=0`, `Не подходит для разбора=1`, normal `5`, gate `passed`. **Rolling-window check:** `call_list_dates=["2026-05-04"]` for all three managers before/after; rolling-window calls did not enter `СПИСОК ВСЕХ ЗВОНКОВ ДНЯ`. **Artifacts:** ready-only PDFs copied to `/tmp/step8j_Эльмира_2026-05-04.pdf`, `/tmp/step8j_Тимур_2026-05-04.pdf`, `/tmp/step8j_Толеген_2026-05-04.pdf`. **New follow-up:** remaining completeness blocker is source audio availability/retention, not quota or manager-facing gate logic; add a bounded source-audio recovery/expiration decision before expecting Эльмира/Тимур gate closure.
- [x] 2026-05-05 — Manager_daily Step 8I: добавлен manager-facing completeness gate для финального daily PDF без изменения analyzer prompts, STT/LLM, scoring, eligibility, selection model, rolling window, coaching_core, Situation Day, Additional Situations, Challenge, Who to work tomorrow, `rop_weekly` или scheduler. **Gate behavior:** обычный manager-facing report разрешён только если в report-day `call_list[]` нет blocking buckets `Без транскрипта`, `Без анализа`, technical `Ошибка анализа` и `Ошибка провайдера`; при fail run/report получает operator-facing `review_required`, reason `incomplete_day_call_processing`, structured `manager_facing_completeness` с counts и affected calls, business email принудительно отключается, Telegram operator/test delivery может остаться preview-only. **Bucket classifier:** persisted failed analyses теперь детерминированно разделяются: `semantic_empty` / `not_coachable_or_reportable` → `Не подходит для разбора` (допустимо в manager report); contract/parser/schema validation → `Ошибка анализа`; provider/quota/rate-limit/timeout/auth/5xx → `Ошибка провайдера`; unknown failed analysis → `Ошибка анализа`. **Ready-only verification без build/delivery на current controlled state 2026-05-04:** Эльмира `meaningful=12`, normal `3`, `Без транскрипта=4`, `Не подходит для разбора=5`, gate `review_required`; Тимур `meaningful=15`, normal `6`, `Без транскрипта=5`, `Не подходит для разбора=4`, gate `review_required`; Толеген `meaningful=6`, normal `4`, `Без анализа=1`, `Не подходит для разбора=1`, gate `review_required`. **Tests:** targeted `docker compose exec -T api pytest -q tests/test_manual_reporting.py -k 'unclassified or call_outcomes or call_list or meaningful or selection_model or completeness or delivery'` — 22 passed.
- [x] 2026-05-05 — Manager_daily Step 8G: `НЕ КЛАСС.` разделён на manager-facing buckets без изменения analyzer prompts, STT/LLM, scoring, eligibility, selection model, rolling window, coaching_core, Situation Day, Additional Situations, Challenge, Who to work tomorrow, `rop_weekly` или scheduler. **Payload/render changes:** internal `status` taxonomy сохранён (`status=None` для звонков без ready outcome); `call_list[]` rows со `status=None` получают `unclassified_status_label` / `unclassified_context_label`; `call_outcomes_summary` сохраняет `unclassified_count` и добавляет `unclassified_by_bucket`; PDF/render model показывает колонку `БЕЗ РАЗБОРА` вместо `НЕ КЛАСС.` и breakdown note под итогами; `СПИСОК ВСЕХ ЗВОНКОВ ДНЯ` показывает конкретные buckets (`Без транскрипта`, `Без анализа`, `Не подходит для разбора`, `Ошибка анализа`, `Нет итога`, `Нет классификации`, `Без разбора`). **Money block:** `ДЕНЬГИ НА СТОЛЕ` считает только actionable outcomes (`Договорённость`, `Открыт`, `Перенос`) и исключает non-actionable buckets. **Ready-only verification без build/delivery на controlled cases 2026-05-04:** Толеген `meaningful=6`, normal outcomes `4`, buckets: `1 без транскрипта`, `1 без анализа`, sum `6`; Тимур `meaningful=15`, normal outcomes `5`, buckets: `6 без транскрипта`, `4 ошибка анализа`, sum `15`; Эльмира `meaningful=12`, normal outcomes `3`, buckets: `5 без транскрипта`, `4 ошибка анализа`, sum `12`. Artifacts copied to host `/tmp/step8g_Толеген_2026-05-04.pdf`, `/tmp/step8g_Тимур_2026-05-04.pdf`, `/tmp/step8g_Эльмира_2026-05-04.pdf`. **Tests:** targeted `docker compose exec -T api pytest -q tests/test_manual_reporting.py -k 'unclassified or call_outcomes or call_list or meaningful or selection_model or money'` — 18 passed; full `docker compose exec -T api pytest -q tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 120 passed; `node --check scripts/generate_docx_report.js` — passed. **Recommendation:** controlled build can continue by best-yield long duration scopes; UI-run still not recommended until delivery semantics / call targeting are safer; future manager-facing category for persisted failed analysis may need refinement if contract-validation vs truly not-coachable should be separated further.
- [x] 2026-05-05 — Manager_daily Step 8E: provider quota circuit breaker and retry guard добавлен для `build_missing_and_report` без изменения analyzer prompts, STT/LLM execution logic, scoring, eligibility, selection model, meaningful/coaching_core rules, Situation Day, Additional Situations, Challenge, Who to work tomorrow, `rop_weekly` или scheduler. **Root cause in code:** `_prepare_artifacts()` в `reporting.py` ловил STT/analysis `ASAError` как обычный failed item (`transcript_build_failed` / `analysis_build_failed`) и продолжал loop по следующим interaction, поэтому `429 insufficient_quota` не останавливал batch. **Реализовано:** добавлен deterministic provider error classifier (`quota_insufficient`, `rate_limited`, `auth_error`, `provider_timeout`, `provider_5xx`, `unknown_provider_error`); OpenAI `429 insufficient_quota` теперь строит structured run-level blocker `type=quota_blocked` с безопасными полями `stage`, `provider`, `account_alias`, `model`, `api_key_env`, `error_class`; после первого quota blocker дальнейшие billable STT/LLM calls в текущем run пропускаются как `quota_blocked_current_run` и учитываются в `processed.skipped_due_to_quota`; response/observability получают top-level `blocker` и `processed` summary. **Retry/idempotency guard:** на interaction сохраняется `metadata_.last_provider_failure` / bounded `provider_failure_history`; следующие `build_missing_and_report` не ретраят такие calls автоматически и помечают их `quota_blocked_previous_run`; explicit operator override добавлен как `force_retry_quota_blocked` в API request и CLI flag `--force-retry-quota-blocked`. **Provider visibility:** response/log-safe metadata показывает alias/env var name без секрета: STT `stt_main` / `OPENAI_API_KEY_STT_MAIN` / `whisper-1`, LLM-1 `llm1_main` / `OPENAI_API_KEY_LLM1_MAIN` / configured model. **Tests:** targeted `pytest -q tests/test_manual_reporting.py -k 'quota or unclassified or call_outcomes or call_list or meaningful or selection_model'` — 18 passed; full `pytest -q tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 120 passed; `node --check scripts/generate_docx_report.js` — passed. **Recommendation:** после top-up повторять не массово; сначала controlled build на 1 менеджере и 1–2 calls, без `force_retry_quota_blocked` для проверки guard, затем с explicit force только для уже помеченных quota-blocked calls.
- [x] 2026-05-05 — Manager_daily Step 8C: structured `Не классифицировано` diagnostics закрыт без изменения analyzer/prompts/STT/LLM/scoring/eligibility/selection/rolling/coaching/pdf labels. **Реализовано**: `ReportArtifact` теперь сохраняет nullable forensic fields `original_analysis` и `analysis_reuse_reason`, чтобы после `_prepare_artifacts` не терять различие между `no_analysis`, `analysis_failed` и `analysis_not_reusable`; добавлены reason labels/codes для unclassified meaningful calls; `payload.unclassified_breakdown` строится только из report-day meaningful layer (`operational_meaningful_artifacts`), не из rolling coaching window; строки `payload.call_list[]` со `status=None` получают `unclassified_reason_code` / `unclassified_reason_label`. **Root cause на свежих 2026-05-04 кейсах**: `Не классифицировано` вызвано не renderer label bug и не Step 8B arithmetic bug, а low ready coverage на report-day meaningful layer: большинство meaningful звонков имеют source-side live signal, но не имеют reusable transcript/analysis после build. Для `manager 304` в локальной DB нет extension `304` на 2026-05-04; acceptance-паттерн `6 meaningful / 6 unclassified` соответствует runtime extension `203` (локального manager mirror row нет). Breakdown после fresh build_missing: ext203/“manager 304 pattern” `raw=24, meaningful=6, analyzed=0, unclassified=6, no_transcript=6`; Толеген `raw=54, meaningful=6, analyzed=1, unclassified=5, no_transcript=5`; Тимур `raw=57, meaningful=15, analyzed=1, unclassified=14, no_transcript=13, no_analysis=1`; Эльмира `raw=41, meaningful=12, analyzed=0, unclassified=12, no_transcript=12`. **build_missing verification**: four fresh `build_missing_and_report` runs for 2026-05-04 returned `partial` because billable STT/LLM-1 attempts hit confirmed `429 insufficient_quota`; `transcripts_built=0`, `analyses_built=0` in all four; failures were `transcript_build_failed:*:STT ... 429 insufficient_quota` and `analysis_build_failed:*:LLM-1 ... 429 insufficient_quota`. This is expected result due to low ready coverage plus external quota blocker, not a renderer/status taxonomy issue. **Artifacts**: generated `manager_daily_preview_203_2026-04-30_manager_daily_template_v2.pdf`, `manager_daily_d42e8246-772e-4a04-bbe7-2b88f45db695_2026-04-30_manager_daily_template_v2.pdf`, `manager_daily_656abe58-7c23-476a-a9f6-d76305cf42e0_2026-04-30_manager_daily_template_v2.pdf`, `manager_daily_09cae83f-7ac1-4ee0-b1d5-3a76c8053c3f_2026-04-30_manager_daily_template_v2.pdf`; all Telegram test delivery paths returned delivered/skipped email. **Tests**: targeted `pytest -q tests/test_manual_reporting.py -k 'unclassified or call_outcomes or call_list or meaningful or selection_model'` — 18 passed; full `pytest -q tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 120 passed; `node --check scripts/generate_docx_report.js` — passed. **Future task**: after diagnostics, consider manager-facing terminology: `meaningful_calls` → live conversations / звонки в списке дня, `Не классифицировано` → Без разбора / Нет готового разбора.
- [x] 2026-05-05 — build_missing_and_report blocker fix Step 8C: insights quote persistence normalization — устранён runtime crash `ProgrammingError: can't adapt type 'dict'` при INSERT в таблицу `insights`. **Root cause**: `_replace_insights()` в `orchestrator.py` (строки 406, 417, 428) передавал `item.get("evidence")` / `item.get("quote")` напрямую в `Insight.quote` (`mapped_column(Text)`). LLM-контракт возвращает `evidence` как `list[dict]` транскрипт-сегментов (`[{start_ms, end_ms, text}]`), что psycopg2 не может сериализовать в `text`. Ошибка не проявлялась для звонков с пустыми `evidence=[]`, но крашила любой run при первом звонке с непустыми сегментами (Тимур Жуматаев, `segments=6`). Два последовательных UI-run (05:31 UTC и 07:37 UTC, 2026-05-05) завершились 500. **Исправление**: добавлена модульная функция `_normalize_insight_quote(value: Any) -> str | None` в `orchestrator.py` (перед `PilotTargetConfig`). Правила нормализации: `None → None`; `str → stripped или None`; `list[dict] → join item["text"]`; `list[str] → join non-empty`; `empty list → None`; `dict → dict["text"] или json.dumps`; `unknown → str() или None`. Все три call-site в `_replace_insights()` обновлены: `quote=item.get("evidence")` → `quote=_normalize_insight_quote(item.get("evidence"))` (strengths, gaps), `quote=item.get("quote")` → `quote=_normalize_insight_quote(item.get("quote"))` (product_signals). **Изменённые файлы**: `core/app/agents/calls/orchestrator.py` (добавлена функция `_normalize_insight_quote`, 3 call-site обновлены); `core/tests/test_manual_reporting.py` (добавлен класс `InsightQuoteNormalizerTests` — 14 тестов на все input-варианты включая crash-reproducer). **Tests**: 98/98 passed (`pytest tests/test_manual_reporting.py`). **Live verification**: `build_missing_and_report` run `date_from=2026-05-04` запущен; взаимодействие `fc630809` (`segments=118`, `stages=3`) — тот тип что вызывал crash — прошло LLM + persist без ошибки и run продолжился на следующих взаимодействиях без `ProgrammingError`. **Scope OUT**: analyzer, prompts, LLM/STT execution, scoring, eligibility, selection model, Step 8B outcome logic, coaching blocks, report templates, rop_weekly, scheduler не изменены.
- [x] 2026-05-05 — Manager_daily content enrichment Step 8B: call_outcomes_summary aligned with report-day meaningful calls выполнен только для `reporting.py` и `report_templates.py`, без изменения analyzer, prompts, LLM/STT, scoring, eligibility, rolling window, coaching_core selection, delivery, `rop_weekly`, scheduler или `scripts/generate_docx_report.js`. **Root cause**: `_build_call_outcomes_summary()` получал `artifacts` (coaching_core, rolling window), тогда как `call_list` строился из `operational_day_artifacts` (report-day meaningful) — отсюда несоответствие ИТОГ ДНЯ и СПИСОК ЗВОНКОВ. **`reporting.py`**: (1) `_build_call_outcomes_summary()` теперь получает `operational_meaningful_artifacts` (report-day meaningful, тот же источник что `call_list`); (2) добавлена явная обработка `analysis is None` → `unclassified_count += 1` (ранее шло в `open` через `_derive_call_status_and_deadline({})`); добавлен `unclassified_count` в return dict; (3) в `build_manager_daily_payload()` строка `call_outcomes_summary = _build_call_outcomes_summary(artifacts=artifacts)` перенесена после вычисления `operational_day_artifacts`; добавлен `operational_meaningful_artifacts = [a for a in operational_day_artifacts if _classify_meaningful_call(a)[0]]`. **`report_templates.py`**: (1) `total_calls` в `_build_manager_daily_model()` теперь берётся из `selection_model.meaningful_calls_total` (падает на `kpi.calls_count` для совместимости со старыми payload); (2) в `outcome_cols` добавлена колонка `НЕ КЛАСС.` если `unclassified_count > 0`. **Coaching blocks не изменены** — `artifacts` (coaching_core) по-прежнему используется для всех coaching-блоков (kpi, level_counts, СИТУАЦИЯ ДНЯ, БАЛЛЫ ПО ЭТАПАМ, РАЗБОР ЗВОНКА и т.д.). **Live verification** (ready-only rebuilds 2026-05-04): Толеген — meaningful=6, outcomes sum=6 (1 agreed + 5 unclass), coaching_core=8; Тимур — meaningful=15, outcomes sum=15 (15 unclass), coaching_core=2; Эльмира — meaningful=12, outcomes sum=12 (12 unclass), coaching_core=4. Все три: `categories_sum == meaningful_calls_total: True`. day_summary outcome_cols verified in sections. Coaching blocks confirmed to use rolling window (coaching_core ≠ meaningful for all three). **Tests**: `pytest tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 106 passed; `node --check scripts/generate_docx_report.js` — passed. Updated test: `call_outcomes_summary["agreed_count"]` assertion changed from 2 to 1 in `test_build_manager_daily_payload_enriches_outcomes_focus_and_dynamics` (correct: only report-day call counted). **Docs**: `MANAGER_DAILY_SELECTION_MODEL.md` updated — ИТОГ ДНЯ section now explicitly documents `call_outcomes_summary` uses `operational_meaningful_artifacts`, `unclassified_count`, and НЕ КЛАСС. column; `BUSINESS_READY_REPORT_PACK_TASKS.md` updated — Step 8B closure section added.
- [x] 2026-05-04 — Manager_daily content enrichment Step 8: СПИСОК ВСЕХ ЗВОНКОВ ДНЯ taxonomy fix выполнен только для блоков `СПИСОК ВСЕХ ЗВОНКОВ ДНЯ` (JS renderer) и вспомогательных функций Python build-layer, без изменения analyzer, prompts, LLM/STT, scoring, eligibility, selection model, readiness, rolling window, delivery, `rop_weekly` или scheduler. **Python** (`reporting.py`): `_build_daily_call_row()` теперь возвращает `status=None` когда `artifact.analysis is None` (вместо fallback `"open"` из `_derive_call_status_and_deadline({})`); возвращает `status="tech_service"` когда `call_type in {support, internal}`; `_derive_call_status_and_deadline()` вызывается только для analyzed non-support/internal звонков. (`report_templates.py`): (1) `_call_status_label()` переписан на чистый mapping без fallback-default: `None/unknown → "Не классифицировано"`, `"rescheduled" → "Перенос"` (было `"Перенесли"`), `"tech_service" → "Тех/сервис"` добавлен; (2) `_call_context_label()`: `status=None → "Нет готового разбора"`; (3) `_manager_status_class()` fallback изменён с `"open"` на `"neutral"`; (4) `_manager_status_fill()` добавлен `"neutral": (240, 240, 240)` и fallback сменён на `.get(...)` с neutral-цветом вместо `mapping[...]`. **JS** (`scripts/generate_docx_report.js`): (1) `allCalls` mapping в `dataFromBundle()` теперь берёт `payload.call_list[i]` по `row[0]-1`, проверяет `call_type in {support, internal} → "Тех/сервис"`, иначе `statusLabelMap[st] || "Не классифицировано"`; при `payloadCall` отсутствии — fallback на `row[5] || "Не классифицировано"`; контекст: если status=`"Не классифицировано"` и rawContext=`"—"` → `"Нет готового разбора"`; (2) `buildCoachingWindowNote()` для rolling window (window_days > 1) теперь добавляет `"В список ниже включены только звонки отчётного дня."`. **Live DB verification**: `_build_daily_call_row()` fix подтверждён на Толеген 2026-04-27 live DB: `type=None,status=None:12` (было `open:12`), `type=support,status=tech_service:1` (было `open:1`), `type=sales_primary,status=agreed:2`, `type=sales_primary,status=open:1`. **Bundle verification**: Толеген 2026-04-27 bundle — строки 1 и 3 (call_type=support) → `"Тех/сервис"`; строки 2 и 5 (agreed) → `"Договорённость"`; строка 4 (open/sales) → `"Открыт"`; fallback `"Нет готового разбора"` корректен. **Architecture gap documented (not fixed)**: `call_outcomes_summary` использует coaching_core (9 звонков, 2 дня), `call_list` — report-day meaningful (5/16 звонков, 1 день) → сумма по статусам в summary ≠ count в list; future task зафиксирован в `BUSINESS_READY_REPORT_PACK_TASKS.md`. pytest `tests/test_manual_reporting.py` — 84 passed; `node --check scripts/generate_docx_report.js` — passed.
- [x] 2026-05-04 — Manager_daily content enrichment Step 7: ДОПОЛНИТЕЛЬНЫЕ СИТУАЦИИ cleanup выполнен только для блока `ДОПОЛНИТЕЛЬНЫЕ СИТУАЦИИ` в `scripts/generate_docx_report.js`, без изменения analyzer, prompts, LLM/STT, scoring, eligibility, selection model, raw/meaningful/coaching_core, readiness, delivery, `rop_weekly`, `report_templates.py` или блоков выше/ниже. Изменения: (1) **`dataFromBundle()` фильтр**: дополнительные ситуации фильтруются по `signal > 0` + непустой title + не `Без названия` + не начинается с `cs_/qp_/nd_/ep_/cl_`; `.slice(0, 3)` гарантирует максимум 3; title больше не оборачивается в `«Ситуация»` при отсутствии; (2) **`buildDopSituatsii()` переписан**: если situations пусто после фильтрации → empty-state `ДОПОЛНИТЕЛЬНЫЕ СИТУАЦИИ` с meta-текстом; если 1 ситуация → heading `ДОПОЛНИТЕЛЬНАЯ СИТУАЦИЯ`; если 2–3 → `ДОПОЛНИТЕЛЬНЫЕ СИТУАЦИИ`; heading не показывается до проверки валидности; (3) **label columns переименованы**: `Ситуация / сигнал` → `Что произошло`; `Что хотел сказать клиент` → `Что это значит`; `Как лучше ответить / что делать` → `Что делать в следующий раз`; `Почему это важно` → `Почему это сработает`; (4) **typeLabel**: убраны emoji-prefixes `✅`/`🔶`, оставлены plain `Сильная сторона` / `Зона роста`; (5) **signal suffix**: `{N} зв.` показывается только если `signal > 0`; не рендерится `0 зв.`. Verification: Эльмира 2026-04-06 bundle — 3 валидных ситуации (signal=5/3/6, titles=`Не фиксирует конкретный следующий шаг`/`Не проверяет, удобно ли говорить`/`Чётко представляется и называет компанию`); heading=`ДОПОЛНИТЕЛЬНЫЕ СИТУАЦИИ`; typeLabel=`Зона роста`/`Зона роста`/`Сильная сторона`; `3 зв.`/`5 зв.`/`6 зв.` без `0 зв.`; row labels renamed; no empty `«Ситуация»`. Толеген 2026-04-27 bundle (signal report, additional_situations=[]) — empty-state renders без ошибок. pytest `tests/test_manual_reporting.py` — 84 passed.
- [x] 2026-05-04 — Manager_daily content enrichment Step 6C: reference-style Situation Day structure выполнен только для блока `СИТУАЦИЯ ДНЯ`, без изменения analyzer, prompts, LLM/STT, scoring, eligibility, selection model, raw/meaningful/coaching_core, readiness, source discovery, delivery, `rop_weekly`, scheduler или блоков ниже `СИТУАЦИЯ ДНЯ`. `reporting.py`: добавлено nullable backward-compatible поле `payload.situation_day_coaching_view` (`pattern_title`, `stage_code`, `stage_label`, `stage_score_label`, `what_happened`, `meaning`, `what_was_missing`, `next_time_action`, `scripts`, `source=deterministic_assembly`, `dialogue_is_partial`), собранное только из existing `focus_stage_deep_dive`, `focus_stage_recommendation`, `situation_evidence_quote`, `situation_dialogue_excerpt` и `score_by_stage`; добавлены deterministic stage-specific scripts без placeholders и без LLM. `scripts/generate_docx_report.js`: `СИТУАЦИЯ ДНЯ` переведена в reference-style coaching flow: `СИТУАЦИЯ ДНЯ · {business-facing pattern_title}` → `Фокусный этап: {stage_name} — {score}/5` → call reference → `Что произошло` → `Фрагмент звонка` → table rows `Что это значит`, `Что не хватило в разговоре`, `Что делать в следующий раз`, `Варианты речёвок`; label `Ошибка менеджера` удалён, old technical rows `Почему это важно`, `Что исправить`, `Минимум на завтра`, `Что сделать в следующих звонках` больше не используются внутри Situation Day. Verification на Толеген 2026-04-27 ready-only без STT/LLM rerun: `raw=67`, `meaningful=16`, `coaching_core=9`; title=`СИТУАЦИЯ ДНЯ · Клиент спрашивает про формат работы, но контекст не уточнён`; focus meta=`Фокусный этап: Квалификация и первичная потребность — 1.9/5`; call reference rendered once; `Что произошло` starts from concrete document-flow quote context; `Фрагмент звонка передан частично: роли участников определены не полностью.`; `Что не хватило в разговоре` present; 3 qualification scripts present; placeholders `[конкретная задача клиента]` and technical codes `cs_`/`qp_`/`nd_` absent in Situation Day slice; blocks below `СИТУАЦИЯ ДНЯ` not changed. Artifacts: `/tmp/tolegen_2026-04-27_6c.docx`, `/tmp/tolegen_2026-04-27_6c.pdf`. Tests: targeted `stage_problem or evidence_quote or focus_stage or recommendation or dialogue_excerpt or situation_day or coaching_core or meaningful or selection_model` — 21 passed; full `tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 106 passed; `node --check scripts/generate_docx_report.js` — passed.
- [x] 2026-05-04 — Manager_daily content enrichment Step 6B: situation dialogue context and layout cleanup выполнен только для блока `СИТУАЦИЯ ДНЯ`, без изменения analyzer, prompts, LLM/STT, scoring, eligibility, selection model, raw/meaningful/coaching_core, readiness, source discovery, delivery, `rop_weekly`, scheduler или блоков ниже `СИТУАЦИЯ ДНЯ`. Feasibility на Толеген 2026-04-27: persisted `interaction.metadata_.segments` доступны и позволяют найти соседние transcript segments вокруг `situation_evidence_quote.client_text`, но speaker roles ненадёжны (`speaker=A` у сегментов), поэтому полноценный диалог не восстанавливается и excerpt честно помечается `is_partial=true`, `partial_reason=speaker_roles_unavailable`. `reporting.py`: добавлено nullable поле `payload.situation_dialogue_excerpt` (`call_id`, `client_name`, `client_phone`, `date_label`, `time_label`, `source`, `is_partial`, `partial_reason`, `turns[]`); matching ищет exact normalized quote в persisted transcript segments, берёт до 1 реплики до/после, matched quote остаётся `speaker=client`, surrounding segments выводятся как `speaker=unknown`; fallback использует только existing `evidence_fragments.manager_text/client_text` и не подставляет `voice_of_customer`. `scripts/generate_docx_report.js`: `СИТУАЦИЯ ДНЯ` перестроена в единую структуру `СИТУАЦИЯ ДНЯ · {stage_name} — {score}/5` → отдельная строка `Звонок: ...` → `Что произошло` → построчный `Фрагмент диалога` → единая таблица `Разбор ситуации` (`Ошибка менеджера`, `Почему это важно`, `Что исправить`, `Минимум на завтра`, checklist `Что сделать в следующих звонках`); standalone `Разбор фокусного этапа`, standalone `Что сделать в следующих звонках` и нижняя таблица `Ошибка менеджера / Что это значит` удалены. Verification на Толеген 2026-04-27 ready-only без STT/LLM rerun: `raw=67`, `meaningful=16`, `coaching_core=9`; `situation_dialogue_excerpt.source=transcript_turns`, `is_partial=true`, turns=`Реплика: обороты устраиваем?` / `Клиент: На бумаге или просто по электронной почте.` / `Реплика: смотрите, я у вас базовый пакет покупаю, надо 180 тысяч`; call reference rendered once as separate line `Звонок: 27 апреля 2026, 11:17 · Азамат · +77082934767`; `Разбор ситуации` present; old standalone headings absent; `Что это значит`, placeholders and technical codes `cs_`/`qp_`/`nd_` absent; Step 2–6A fields preserved. Artifacts: `/tmp/tolegen_2026-04-27_6b.docx`, `/tmp/tolegen_2026-04-27_6b.pdf`. Tests: targeted `stage_problem or evidence_quote or focus_stage or recommendation or dialogue_excerpt or coaching_core or meaningful or selection_model` — 21 passed; full `tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 106 passed; `node --check scripts/generate_docx_report.js` — passed.
- [x] 2026-05-04 — Manager_daily content enrichment Step 6A: top-to-situation consistency cleanup выполнен только для верхних блоков до `СИТУАЦИЯ ДНЯ` включительно, без изменения analyzer, prompts, LLM/STT, scoring, eligibility, selection model, raw/meaningful/coaching_core, readiness, source discovery, delivery, `rop_weekly`, scheduler или блоков ниже `СИТУАЦИЯ ДНЯ`. `scripts/generate_docx_report.js`: (1) под `БАЛЛЫ ПО ЭТАПАМ` удалён старый sub-block `Фокус на завтра: {stage}` с checklist-like weak criteria; оставлена только таблица этапов и одна поясняющая строка; (2) добавлены helpers для единого call reference `Звонок: {дата}, {время} · {имя} · {телефон}` без пустых placeholders и без дублирования телефона как ФИО; (3) `Что произошло` теперь собирается из `situation_evidence_quote` + `focus_stage_deep_dive`, а не из meta-текста `Главный сигнал дня / Приоритетный этап / Паттерн`; (4) `Фрагмент диалога` показывает реквизиты звонка перед реальной quote; (5) `Ошибка менеджера` берёт `focus_stage_deep_dive.what_went_wrong` / priority `problem_summary`, а не positive label; (6) таблица `СИТУАЦИЯ ДНЯ` упрощена до `Ошибка менеджера` + `Что это значит`, удалены `Что делать в следующий раз`, `Варианты речёвок`, `Почему работает`; (7) `ДЕНЬГИ НА СТОЛЕ` переведён на 3 колонки `Категория / Кол-во / Потенциал`, удалён столбец `Средний чек`, пояснение использует `80 000 тенге` вместо `80 000 ₸`. `reporting.py`: `situation_evidence_quote` расширен safe metadata `client_phone` и `date_label` для renderer-only call reference. Verification на Толеген 2026-04-27 ready-only без STT/LLM rerun: `raw=67`, `meaningful=16`, `coaching_core=9`; old focus subblock absent; `Что произошло`=`Звонок: 27 апреля 2026, 11:17 · Азамат · +77082934767...`; `Фрагмент диалога` содержит тот же call reference + `Клиент: На бумаге или просто по электронной почте.`; `Ошибка менеджера`=`Роль собеседника не была уточнена.`; placeholders `[конкретная задача клиента]` absent; `Средний чек` column absent; `80 000 тенге` present; technical codes `cs_`/`qp_`/`nd_` absent; Step 2–5 fields preserved. Artifacts: `/tmp/tolegen_2026-04-27_6a.docx`, `/tmp/tolegen_2026-04-27_6a.pdf`. Tests: targeted `stage_problem or evidence_quote or focus_stage or recommendation or coaching_core or meaningful or selection_model` — 19 passed; full `tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 104 passed; `node --check scripts/generate_docx_report.js` — passed.
- [x] 2026-05-04 — Manager_daily content enrichment Step 5: добавлена stage-linked recommendation для фокусного этапа без изменения analyzer, prompts, LLM/STT, scoring, eligibility, selection model, rolling window, delivery, `rop_weekly` или scheduler. `reporting.py`: добавлено nullable поле `payload.focus_stage_recommendation` (`stage_code`, `stage_name`, `problem`, `recommendation`, `checklist`, `source`) из `focus_stage_deep_dive` с deterministic checklist fallbacks по `stage_code`; source выставляется `focus_stage_deep_dive`, `recommendations` или `stage_fallback`. `scripts/generate_docx_report.js`: в `СИТУАЦИЯ ДНЯ` после `Разбор фокусного этапа` добавлен compact subsection `Что сделать в следующих звонках`; если recommendation дословно совпадает с уже показанным `Что исправить`, renderer показывает только checklist и не дублирует текст. Verification на Толеген 2026-04-27 ready-only без STT/LLM rerun: `raw=67`, `meaningful=16`, `coaching_core=9`, `readiness=signal_report`, `window=2026-04-24..2026-04-27`; focus recommendation stage=`qualification_primary`, source=`focus_stage_deep_dive`, recommendation=`До презентации задать 2–3 уточняющих вопроса и только потом связывать продукт с задачей клиента.`, checklist=`Уточнить роль собеседника` / `Понять текущий процесс` / `Зафиксировать следующий шаг`; DOCX check: `Что произошло`=1, `Фрагмент диалога`=1, `Разбор фокусного этапа`=1, `Что сделать в следующих звонках`=1, checklist items present, technical codes `cs_`/`qp_`/`nd_`=0, Step 2/3/4 fields preserved. Artifacts: `/tmp/tolegen_2026-04-27_stage_rec.docx`, `/tmp/tolegen_2026-04-27_stage_rec.pdf`. Tests: targeted `stage_problem or evidence_quote or focus_stage or recommendation or coaching_core or meaningful or selection_model` — 19 passed; full `tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 104 passed; `node --check scripts/generate_docx_report.js` — passed.
- [x] 2026-05-04 — Manager_daily content enrichment Step 4: добавлен compact `focus_stage_deep_dive` для `СИТУАЦИЯ ДНЯ` без изменения analyzer, prompts, LLM/STT, scoring, eligibility, selection model, rolling window, delivery, `rop_weekly` или scheduler. `reporting.py`: добавлено nullable поле `payload.focus_stage_deep_dive` (`stage_code`, `stage_name`, `what_went_wrong`, `why_it_matters`, `what_to_fix`, `minimum_for_tomorrow`) из existing `score_by_stage[].problem_summary`, `key_problem_of_day`, stage-specific deterministic fallbacks и existing recommendations только как safe backup; `why_it_matters` берёт `key_problem.description` только если текст похож на manager-facing explanation, иначе использует stage-specific fallback. `scripts/generate_docx_report.js`: в `СИТУАЦИЯ ДНЯ` после `Фрагмент диалога` добавлен compact subsection `Разбор фокусного этапа` с 4 строками (`Что пошло не так`, `Почему это проблема`, `Что исправить`, `Минимум на завтра`); пустой deep-dive не рендерится, старые bundles остаются совместимы. Verification на Толеген 2026-04-27 ready-only без STT/LLM rerun: `raw=67`, `meaningful=16`, `coaching_core=9`, `readiness=signal_report`, `window=2026-04-24..2026-04-27`; focus stage=`qualification_primary`; what=`Роль собеседника не была уточнена.`; why=`Без понимания роли, процесса и задачи клиента презентация звучит общей и не привязана к реальной потребности.`; fix=`До презентации задать 2–3 уточняющих вопроса и только потом связывать продукт с задачей клиента.`; minimum=`В каждом подходящем sales-звонке зафиксировать роль собеседника, текущий процесс и следующий шаг.`; DOCX check: `Что произошло`=1, `Фрагмент диалога`=1, `Разбор фокусного этапа`=1, four deep-dive row labels present, technical codes `cs_`/`qp_`/`nd_`=0, Step 2/3 fields preserved. Artifacts: `/tmp/tolegen_2026-04-27_focus_stage.docx`, `/tmp/tolegen_2026-04-27_focus_stage.pdf`. Tests: targeted `stage_problem or evidence_quote or focus_stage or coaching_core or meaningful or selection_model` — 19 passed; full `tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 104 passed; `node --check scripts/generate_docx_report.js` — passed.
- [x] 2026-05-04 — Manager_daily content enrichment Step 3: `СИТУАЦИЯ ДНЯ` получила stage-linked real evidence quote из existing `evidence_fragments` без изменения analyzer, prompts, LLM/STT, scoring, eligibility, selection model, rolling window, delivery, `rop_weekly` или scheduler. `reporting.py`: добавлено nullable поле `payload.situation_evidence_quote` (`client_text`, optional `manager_text`, `criterion_code`, `stage_code`, `source=evidence_fragments`, `call_id`, `client_label`, `time_label`); quote выбирается только если `evidence_fragments[].criterion_code` prefix совпадает с priority stage, с приоритетом exact key-problem criterion → weak criterion фокусного этапа → любой quote priority stage; случайные `voice_of_customer` quotes не используются. `scripts/generate_docx_report.js`: в `СИТУАЦИЯ ДНЯ` после `Что произошло` добавлен блок `Фрагмент диалога`; renderer показывает `Менеджер:` + `Клиент:` если есть обе стороны, только `Клиент:` если есть только `client_text`, либо честный fallback `Фрагмент диалога в текущем payload не передан.`; цитаты ограничиваются по длине и technical codes не выводятся. Verification на Толеген 2026-04-27 ready-only без STT/LLM rerun: `raw=67`, `meaningful=16`, `coaching_core=9`, `readiness=signal_report`, `window=2026-04-24..2026-04-27`; quote found = yes, source=`evidence_fragments`, stage=`qualification_primary`, criterion=`qp_current_process`, rendered text=`Клиент: На бумаге или просто по электронной почте.`; DOCX check: `Что произошло`=1, `Фрагмент диалога`=1, technical codes `cs_`/`qp_`/`nd_`=0, Step 2 `Основная проблема`/`problem_summary` preserved. Artifacts: `/tmp/tolegen_2026-04-27_evidence_quote.docx`, `/tmp/tolegen_2026-04-27_evidence_quote.pdf`. Tests: targeted `stage_problem or evidence_quote or coaching_core or meaningful or selection_model` — 18 passed; full `tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 103 passed; `node --check scripts/generate_docx_report.js` — passed.
- [x] 2026-05-04 — Manager_daily content enrichment Step 2: stage-specific problem summaries добавлены в `manager_daily` payload и DOCX/PDF renderer без изменения analyzer, analyzer prompts, LLM logic, scoring, eligibility, selection model, rolling window, delivery, `rop_weekly` или scheduler. `reporting.py`: `_aggregate_stage_scores()` теперь использует `criterion_code` stage-prefix (`cs_*`, `qp_*`, `nd_*`, etc.) и existing `criteria_results.comment` / `gaps` / `evidence_fragments` для поля `score_by_stage[].problem_summary` + `problem_source`; `_aggregate_finding_items()` сохраняет `criterion_code` в aggregated findings. `scripts/generate_docx_report.js`: колонка `БАЛЛЫ ПО ЭТАПАМ → Основная проблема` берёт `stage.problem_summary`, fallback остаётся только если данных нет; priority stage больше не теряет stage-specific summary и использует `key_problem_of_day.title` только как fallback; исправлен двойной префикс `Не не…` в focus criteria. Verification на Толеген 2026-04-27 ready-only без STT/LLM rerun: `raw=67`, `meaningful=16`, `coaching_core=9`, `readiness=signal_report`, `window=2026-04-24..2026-04-27`; stage summaries surfaced для Э1/Э2/Э3/Э4/Э6, fallback count в `БАЛЛЫ ПО ЭТАПАМ` = 0, technical codes `cs_`/`qp_`/`nd_` в DOCX = 0. Artifacts: `/tmp/tolegen_2026-04-27_stage_problem.docx`, `/tmp/tolegen_2026-04-27_stage_problem.pdf`. Tests: targeted `stage_problem or coaching_core or meaningful or selection_model` — 16 passed; full `tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 101 passed.
- [x] 2026-05-04 — Payload feasibility audit (Веха 6.5, manager_daily content enrichment Step 1): проведён readonly feasibility audit текущего механизма для content enrichment блоков БАЛЛЫ ПО ЭТАПАМ и СИТУАЦИЯ ДНЯ. Верифицировано на Толеген Жангазиев 2026-04-27 (67 raw → 16 meaningful → 9 eligible coaching в 2-дневном rolling window 27.04+24.04). **Ключевое открытие:** `evidence_fragments` и `gaps` в DB содержат поле `criterion_code` с stage-prefix (`cs_*→contact_start`, `qp_*→qualification_primary`, `nd_*→needs_discovery`) — это de facto stage-linkage механизм, не используемый текущей агрегацией. Per-stage problem summaries и stage-linked evidence quotes можно получить без изменения analyzer, только через изменение `_aggregate_stage_scores()` и `_aggregate_finding_items()` в `reporting.py`. Реальные клиентские цитаты (`client_text` non-null) найдены в 7 из 16 evidence_fragments Толегена. **Матрица feasibility:** `stage_problem_summary_by_stage` — PARTIAL (data в DB, не в payload); `focus_stage_deep_dive` — PARTIAL (общий совет через существующие поля, stage-specific требует механизма); `situation_what_happened` — AVAILABLE (уже реализовано); `situation_evidence_quote` — PARTIAL/UNSAFE (data в DB, не surfaced как stage-linked); `manager_error_summary` — AVAILABLE (уже реализовано); `next_time_action_advice` — AVAILABLE (уже реализовано). Docs обновлены: `BUSINESS_READY_REPORT_PACK_TASKS.md` — новый раздел `Manager_daily Content Enrichment — Feasibility Audit Results` с матрицей, ключевым открытием, verified Tolegen state.
- [x] 2026-05-04 — Manager daily: БАЛЛЫ ПО ЭТАПАМ + СИТУАЦИЯ ДНЯ content improvements (Веха 6.5): устранены reader-facing проблемы в двух coaching-блоках без изменений analyzer, LLM logic, eligibility, selection rules, rolling window, Telegram/email delivery или rop_weekly. `scripts/generate_docx_report.js`: (1) **БАЛЛЫ ПО ЭТАПАМ — колонка "Основная проблема"**: заголовок переименован с «Что это значит» на «Основная проблема»; добавлена функция `stageProblem(stage)` — для priority stage возвращает `DATA.key_problem.title` (из `payload.key_problem_of_day`), для non-priority stages — fallback «Недостаточно данных для конкретного вывода по этапу.»; `stageMeaning()` удалена как неиспользуемая; (2) **БАЛЛЫ ПО ЭТАПАМ — блок "Фокус на завтра"**: заголовок переименован с «Что просело в фокусном этапе:» на «Фокус на завтра: {stageName}:»; добавлена функция `criterionToProblem(name)` — простая негация: «Не» + lowercase первый символ + остаток; слабые критерии теперь показываются в проблемной форме (например: «Не понял приоритет и срок»); (3) **СИТУАЦИЯ ДНЯ — реструктуризация**: убраны строки «Главный сигнал дня» (тело переходит в pre-table параграф), «Приоритетный этап», «Пример из сегодня»; добавлен pre-table параграф «Что произошло: {bodyText}» (оранжевый, если bodyText есть); добавлена строка «Ошибка менеджера» = `DATA.key_problem.title` (жирный красный, условно если доступен); строка «Что хотел сказать клиент» переименована в «Что это значит»; строка «Наша задача» переименована в «Что делать в следующий раз»; «Варианты речёвок» и «Почему работает» сохранены без изменений; (4) **`dataFromBundle()`** расширен: `key_problem: { title, description }` из `payload.key_problem_of_day`; (5) **`emptyStateData()`** расширена: `key_problem: { title: "", description: "" }`. Verification: Эльмира 2026-04-06 bundle — `[✓]` БАЛЛЫ: заголовок «Основная проблема», priority row = key_problem.title, non-priority = fallback; фокус-блок «Фокус на завтра: Э3 Выявление потребностей:» с негированными критериями; СИТУАЦИЯ ДНЯ: pre-table «Что произошло:», строка «Ошибка менеджера», нет «Приоритетный этап»/«Пример из сегодня». Толеген 2026-04-27 bundle (signal report, stages=[], key_problem пустой) — `[✓]` нет ошибок, БАЛЛЫ ПО ЭТАПАМ не рендерится, СИТУАЦИЯ ДНЯ empty-fallback. pytest `tests/test_manual_reporting.py` — 78 passed.
- [x] 2026-05-04 — Manager daily: header/dashboard/money cleanup (Веха 6.5, roadmap step: manager_daily top summary and money block cleanup): устранены 5 reader-facing проблем в шапке, service note, верхнем dashboard и блоке ДЕНЬГИ НА СТОЛЕ без изменений analyzer, eligibility, rolling window, readiness logic, Telegram/email delivery, rop_weekly или scheduler. `scripts/generate_docx_report.js`: (1) **Шапка** — добавлен `formatRussianDate()` (форматирует дату в «27 апреля 2026», обрабатывает range-строки вида date1..date2 → берёт последнюю), `report_day` выводится в manager-facing формате, счётчик показывает «N содержательных звонков» вместо «N звонков»; (2) **Тип отчёта** — добавлен compact badge «Сигнальный отчёт» / «Полный отчёт» между именем менеджера и баллом дня, получается из readiness_outcome или prefix selection_note; (3) **Service note** переписана в двухчастную структуру: `buildDayFunnelNote()` строит воронку дня «Воронка дня: найдено в телефонии — N; содержательных — M; исключено из списка дня — K.» с причинами исключения (только если сумма сходится), `buildCoachingWindowNote()` строит коучинговую заметку «В коучинговый разбор вошло N из M звонков дня.» (rolling window: «Коучинговый разбор собран по расширенной базе за N рабочих дня. В разбор вошло N звонков.»); техническая service note из selection_note payload больше не показывается; (4) **Dashboard `buildSvodnaya()`** — total = `DATA.meaningful_calls` (все содержательные звонки отчётного дня), label «ИТОГ ДНЯ», добавлена ячейка «НЕ КЛАСС.» если сумма outcome < meaningful; caption поясняет слой; (5) **ДЕНЬГИ НА СТОЛЕ `buildDengi()`** — полностью переписан: таблица Договорённость/Открыт/Перенос × 80 000 ₸ средний чек = потенциал, строка Итого, пояснение о предварительной оценке; если qualifying = 0 → «Данных для данного раздела недостаточно.»; никаких технических fallback-строк. `dataFromBundle()` расширен: `report_day`, `report_type`, `day_funnel`, `coaching_window`. `core/app/agents/calls/report_templates.py`: (6) `_build_money_on_table_data()` — убраны «runtime contract», «требует внешней CRM», «structural fallback», «Блок сохранён в формате v5»; новая логика: AVG_CHECK 80 000 ₸, тело из agreed+open+rescheduled counts, fallback «Данных для данного раздела недостаточно.»; (7) `_build_manager_daily_model()` — добавлено поле `report_type` в секцию `report_header` из readiness_outcome. Создан verification bundle `verification_tolegen_2026-04-27_bundle.json` из debug-данных Толегена для воспроизводимого тестирования. Rebuilt artifacts: `/tmp/tolegen_2026-04-27.docx` (13.3 KB), `/tmp/elmira_2026-04-06.docx` (18.6 KB). Verification: шапка Толеген «27 апреля 2026  ·  5 содержательных звонков», badge «Сигнальный отчёт», воронка «Воронка дня: найдено в телефонии — 5.», коучинг «В коучинговый разбор вошло 4 из 5 звонков дня.», dashboard total=5, ДЕНЬГИ — таблица 80 000 ₸ без технических строк; PIPELINE ТЁПЛЫХ ЛИДОВ — отсутствует, УТРЕННЯЯ КАРТОЧКА — отсутствует; Эльмира backward compat — дата «6 апреля 2026», нет badge, ДЕНЬГИ таблица 80 000 ₸; pytest `coaching_core or meaningful or selection_model` — 15 passed; full regression `tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 100 passed.
- [x] 2026-05-03 — Challenge card + morning card removal (Веха 6.5): `scripts/generate_docx_report.js` — два точечных изменения без правок payload contract, analyzer, reporting.py или тестов. (1) **ЧЕЛЛЕНДЖ НА ЗАВТРА → labeled card**: `buildChellendj()` переписан как двухколоночная amber-карточка с labeled rows: «Цель» (`goal_line`, bold navy), «Фокус на завтра» (`today_line` + `record_line`, gray), «Фраза для завтра» (`phrase_line`, italic navy); пустые строки отфильтровываются через conditional rows; label cells используют `FFE082`, content cells — `FFFBEA`; заголовок блока сохраняет dark navy shading от предыдущего cleanup. (2) **УТРЕННЯЯ КАРТОЧКА убрана из PDF/DOCX**: `buildUtrennaya()` удалена из сборки `children[]`; payload `DATA.morning` и Telegram delivery не затронуты; функция-тело сохранена в коде с комментарием для трассировки. Rebuilt artifact от case bundle: `/tmp/case_challenge_v1.docx` (18.5 KB), `/tmp/case_challenge_v1.pdf` (162 KB). Verification: все 17 XML-checks (challenge labels, amber fills, absent УТРЕННЯЯ КАРТОЧКА, regression check предыдущих fixes) — pass; pytest selection tests — 15 passed; full regression — 100 passed.
- [x] 2026-05-03 — Visual readability cleanup (Веха 6.5): `scripts/generate_docx_report.js` — четыре класса правок без изменений payload contract, analyzer, reporting.py или тестов. (1) **Section header backgrounds**: `blockHeading()` теперь имеет `shading: {fill: "1F3864"}` + белый текст на всех section-level заголовках. (2) **Table header row contrast**: `COLORS.tableHead` потемнён с `E8F0F8` до `C8DCF0`; добавлен `COLORS.red = "C0392B"` (устранена undefined-ref в `buildSituatsiya` и `buildPozvoni`). (3) **СИТУАЦИЯ ДНЯ 2-column table**: `buildSituatsiya()` переписан — выводит labeled 2-column table с 7 строками («Главный сигнал дня», «Приоритетный этап», «Что хотел сказать клиент», «Наша задача», «Пример из сегодня», «Варианты речёвок», «Почему работает»); добавлены helper-функции `labelCell()` и `cellMultiPara()`; пустые строки (null-условия) отфильтровываются через `.filter(Boolean)`; `body` fallback расширен до `situation.body || situation.text` для совместимости с API-response bundles. (4) **ДОПОЛНИТЕЛЬНЫЕ 3 СИТУАЦИИ per-situation tables**: `buildDopSituatsii()` рендерит каждую ситуацию как compact 4-row table («Ситуация / сигнал», «Что хотел сказать клиент», «Как лучше ответить / что делать», «Почему это важно»); исправлен data mapping: `meant` fallback на `item.interpretation`, фильтр `signal > 0` удалён (sections-format items не имеют `signal`), `type` инферируется из `item.badge` при отсутствии `item.kind`. Rebuilt DOCX/PDF artifact от canonical case bundle: `/tmp/case_visual_v3.docx` (19.2 KB), `/tmp/case_visual_v3.pdf` (164 KB). Verification: все 14 XML-checks (shading, labels, headers) — pass; pytest `tests/test_manual_reporting.py -k coaching_core or meaningful or selection_model` — 15 passed.
- [x] 2026-05-03 — Manager-facing clarity cleanup (Веха 6.5): упрощён presentation layer `manager_daily` DOCX/PDF без изменений payload contract, analyzer, raw/meaningful/coaching_core, readiness, eligibility, source discovery, scheduler или `rop_weekly`. В `scripts/generate_docx_report.js` больше не рендерится CRM-like блок тёплых лидов; action block переименован в `КОГО ВЗЯТЬ В РАБОТУ ЗАВТРА` и перестроен в таблицу `Приоритет / Клиент / Ситуация или договорённость / Рекомендация / Первая фраза` на уже существующих `call_tomorrow`/call-list данных с bounded fallbacks; пустой action list теперь показывает короткую manager-facing фразу без пустой таблицы. `БАЛЛЫ ПО ЭТАПАМ` упрощены до `Этап / Балл / Статус / Что это значит`: удалены `Среднее`, visual scale, symbol statuses и debug subcriteria rows с `0.0`; добавлен manager-facing поясняющий текст про фокус на завтра и короткий блок 1–2 проблем фокусного этапа, только если они безопасно извлекаются из existing criteria data. Rebuilt smoke artifact из existing Тимур 2026-04-28 clean JSON без STT/LLM rerun: `/tmp/manager_daily_656abe58-7c23-476a-a9f6-d76305cf42e0_2026-04-28_clarity_cleanup.pdf` plus DOCX. Verification: DOCX XML smoke confirmed pipeline block removed, action table renamed/structured with 5 rows, stage table simplified, debug `0.0` criteria removed, and header/footer count fix still `22`; targeted manager_daily selection tests — 15 passed; full reporting/provider regression — 100 passed.
- [x] 2026-05-03 — PDF count consistency fix (Веха 6.5): устранён `manager_daily` presentation mismatch в DOCX/PDF generator после analyzer guardrail verification. `scripts/generate_docx_report.js` теперь хранит отдельный `meaningful_calls` из `payload.selection_model.meaningful_calls_total` с fallback на `DATA.all_calls.length`; header показывает manager-facing count meaningful day calls, а footer `СПИСОК ВСЕХ ЗВОНКОВ ДНЯ` считает фактические строки `DATA.all_calls.length`. Coaching/report-core счетчик `DATA.calls` оставлен для coaching-related blocks и outcome table; Python payload assembly, analyzer, eligibility, coaching_core, readiness, source discovery и raw/meaningful selection rules не менялись. Rebuilt smoke artifact from existing Тимур 2026-04-28 clean JSON without STT/LLM rerun: `/tmp/manager_daily_656abe58-7c23-476a-a9f6-d76305cf42e0_2026-04-28_js_count_fix.pdf` plus DOCX. Verification: DOCX XML smoke confirmed header `22 звонка`, call-list rows `22`, footer `Показаны все 22 звонка`, and no old `6 звонков` header; targeted manager_daily selection tests — 15 passed; full reporting/provider regression — 100 passed.
- [x] 2026-05-03 — Verification rerun (Веха 6.5): analyzer eligibility guardrail проверен на трёх suspicious calls Тимура Жуматаева за 2026-04-28 без повторного STT/source rebuild для самих звонков (использованы persisted transcripts). `e1ae6c25` после rerun больше не имеет противоречия `score=75 + not_eligible`: analyzer вернул `not_coachable_or_reportable`, persisted failed analysis, `score_percent=0`, вне coaching_core. `c77db598` repaired by guardrail: `mixed/warm_webinar_or_lead`, `eligible`, `eligibility_reason=positive_sales_score_with_sales_evidence`, `score_percent=50`, вошёл в coaching_core. `3220af59` остался объяснимо excluded: `sales_primary/cold_outbound`, `not_eligible`, `duration_below_threshold`, `score_percent=0`, `not_coachable_or_reportable`, вне coaching_core. На sub-180s calls ложного `duration_ge_180_sec...` после rerun нет. Rebuilt Тимур `manager_daily` from ready data with delivery disabled for business email: `raw_calls_total=35`, `meaningful_calls_total=22`, `included_in_report_total/coaching_core=6` (было 5 по диагностике), `readiness_outcome=signal_report`, `window_days_used=1`; artifact saved locally as `/tmp/manager_daily_656abe58-7c23-476a-a9f6-d76305cf42e0_2026-04-28_after_guardrail.pdf` plus `/tmp/timur_2026-04-28_after_guardrail_report_clean.json`. Stage scores shifted only at `contact_start` in comparable simulated payload (`8.5 -> 7.9`); priority stage remained `completion_next_step`; `signal_of_day` stayed on +77017230909, while `key_problem_of_day` shifted from pain discovery to checking call relevance. Verification: guardrail tests — 3 passed; manager_daily selection tests — 15 passed; full reporting/provider regression — 100 passed.
- [x] 2026-05-03 — Bounded guardrail (Веха 6.5): stabilized `manager_daily` coaching_core eligibility at analyzer normalization layer. Root cause: `build_contract_template()` defaulted to `analysis_eligibility=eligible` / `eligibility_reason=duration_ge_180_sec_and_sales_relevant`, while post-LLM normalization recomputed checklist score but did not reconcile classification, score evidence, and actual `duration_sec`; LLM output could therefore persist contradictions like sales-relevant `score_percent > 0` with `analysis_eligibility=not_eligible`, or `duration_ge_180_sec...` reason on sub-180s calls. Added deterministic post-score guardrail in `CallsAnalyzer._validate_and_normalize_contract()`: support/internal/other stay `not_eligible`; sales-relevant calls marked `not_eligible` are repaired to `eligible` only when positive checklist score and existing scoring/evidence content are already present; not-eligible calls without sales evidence keep zero checklist score; duration reason is repaired when actual duration is below configured 180s threshold. Scope did not rerun real analyses or rebuild reports, and did not change raw/meaningful selection, coaching_core selection, renderer, readiness thresholds, scheduler, or `rop_weekly`. Verification: analyzer guardrail tests — 3 passed; `tests/test_manual_reporting.py -k 'coaching_core or meaningful or selection_model'` — 15 passed; `tests/test_ai_provider_routing.py` — 22 passed.
- [x] 2026-05-03 — Documentation/Git close-out (Веха 6.5): canonical docs синхронизированы с implemented `manager_daily meaningful_calls` recalibration. `docs/MANAGER_DAILY_SELECTION_MODEL.md` теперь явно фиксирует transcript-first rule и CDR-only / no-transcript rule (`source_status=answered`, `direction in/out`, `duration >= 90`), а также то, что short/missed/local/routing-like no-transcript rows остаются в `raw_calls`, но исключаются из `meaningful_calls` как `too_short_or_no_speech`; `raw_calls` и `coaching_core` semantics не менялись. `docs/MANUAL_REPORTING_PILOT.md` обновлён краткой синхронизированной формулировкой. `DECISIONS.md` не обновлялся: это уточнение ADR-048/canonical model, а не новое standing decision.
- [x] 2026-05-03 — Bounded refinement (Веха 6.5): recalibrated `meaningful_calls` после снятия hardcoded source-side duration cutoff. Root cause: SM-2 default/R5 считал любой no-transcript CDR с `duration >= 15` meaningful; пока source discovery был урезан `talk_duration >= 180`, это почти не проявлялось, но после расширения raw source-day начало добавлять partial contact / short answered / routing-like no-transcript rows в manager-facing list. `_classify_meaningful_call()` теперь сохраняет transcript-first rule, support/internal с transcript остаётся meaningful, IVR/other not_eligible без transcript остаётся исключением, а CDR-only no-transcript звонок проходит только как probable live conversation при `source_status=answered`, `direction in/out`, `duration >= 90`; missed/local/short no-transcript попадают в `too_short_or_no_speech`. Raw/source counters, UI duration filters, analyzer, coaching_core, renderer, readiness thresholds, scheduler и `rop_weekly` не менялись. Live CDR check без записи в БД: Тимур 2026-04-28 ext 311 — raw 35, meaningful 17 → 9, coaching 3; исключены 8 answered no-transcript 15–89с плюс missed/no-speech rows, сохранены 4 transcript calls и 5 no-transcript >=90с. Толеген 2026-04-27 ext 325 — raw 67, meaningful 34 → 16, coaching 3; исключены 18 answered no-transcript 15–89с плюс missed/no-speech rows, сохранены 5 transcript calls и 11 no-transcript >=90с. Verification: targeted meaningful/selection/source tests — 15 passed.
- [x] 2026-05-03 — Bounded fix (Веха 6.5): для `manager_daily` убран hardcoded source-side duration cutoff `talk_duration >= 180` из source discovery path. Root cause previous diagnostic был в том, что `_discover_and_persist_source_calls()` вызывал `intake.filter_eligible(records)` до manager/extension scope, из-за чего `raw_calls_total` стартовал уже с урезанного source subset. Теперь source discovery сначала берёт полный CDR scope по выбранному manager/extension/date, затем применяет только явные UI duration filters (`min_duration_sec` / `max_duration_sec`). Structured diagnostics/observability расширены source funnel полями: `source_raw_calls_total`, `source_calls_after_ui_filters_total`, `eligible_source_records_total` как build-eligible subset, далее selected interactions / meaningful / coaching. Для build_missing path source-only missed/no-recording calls больше не отправляются в audio/STT build. Проверка live CDR без записи в БД: Тимур 2026-04-28 ext 311 — source scope 35, after UI filters 35, meaningful 17, coaching 3; явный UI `min_duration_sec=180` возвращает 4. Толеген 2026-04-27 ext 325 — source scope 67, after UI filters 67, meaningful 34, coaching 3; явный UI `min_duration_sec=180` возвращает 5. Scope не менял meaningful SM-2 rules, coaching_core rules, analyzer, renderer, readiness thresholds, scheduler или `rop_weekly`. Verification: targeted source/selection tests — 15 passed; full relevant regression `tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 97 passed.
- [x] 2026-04-30 — Bounded bugfix (Веха 6.5): `support/internal + analysis_eligibility=not_eligible` больше не попадает в `manager_daily` coaching_core / final included set. Root cause подтверждён как runtime selection bug: `_split_usable_artifacts()` считал coaching_core по факту `transcript + reusable analysis` и не проверял coaching eligibility, поэтому persisted support/not_eligible analysis мог войти в `usable`. Добавлен manager_daily-only guard `require_coaching_core_eligible` и helper `_is_coaching_core_eligible()`: такие звонки остаются доступны в `window_artifacts` для `meaningful_calls`, `СПИСОК ЗВОНКОВ ДНЯ` и counters, но исключаются из coaching blocks/readiness included set. Реальный кейс Толеген Жангазиев / 2026-04-27 проверен: `+77089089101` теперь `meaningful=True`, `ready_analysis=True`, `coaching_core=False`; sales-звонки дня остались в coaching_core; `Ирина` остаётся meaningful и вне core (`not_coachable_or_reportable`). После фикса Толеген: окно 1 день `raw=5/core=3 -> skip_accumulate`, окно 2 дня `raw=8/core=4 -> signal_report`, то есть signal logic сохраняется. Verification: targeted selection tests — 20 passed; full relevant regression `tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 97 passed.
- [x] 2026-04-30 — SM-6 (Веха 6.5): финальная acceptance-проверка canonical selection model на известных manager/day cases. Проверены persisted known cases: Тимур Жуматаев / 28.04 (`raw=4`, `meaningful=4`, `coaching_core=3`, call list=4, `signal_report`, window=1), Толеген Жангазиев / 27.04 (`raw=5`, `meaningful=5`, `coaching_core=4`, call list=5, `signal_report`, window=2: 24.04..27.04), Эльмира Кешубаева / 28.04 (`raw=6`, `meaningful=6`, `coaching_core=5`, call list=6, `full_report`, window=1). Acceptance выявил и сразу закрыл один selection-model gap: при expanded coaching window service note считал `raw_calls_total` по coaching window, а не по выбранному operational day; `build_manager_daily_payload()` теперь строит `selection_model` по operational day artifacts, while coaching blocks/readiness remain on coaching window. Подтверждено: `СПИСОК ЗВОНКОВ ДНЯ` строится из meaningful layer выбранного дня; coaching blocks используют `coaching_core` (`kpi_calls_count == coaching_core_total`, call list может быть шире); rolling transparency появляется только при `window_days_used > 1`; при окне 1 день лишней фразы нет; full/signal framing читается корректно. Rebuilt artifacts: `/tmp/ai_sales_analyzer_sm6/timur_2026_04_28_signal_report.pdf`, `/tmp/ai_sales_analyzer_sm6/tolegen_2026_04_27_signal_report.pdf`, `/tmp/ai_sales_analyzer_sm6/elmira_2026_04_28_full_report.pdf` плюс JSON summaries. Verification: targeted SM-4/SM-5/expanded-window tests — 7 passed; full regression `tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 97 passed. Overall verdict: SM-6 passed; no remaining selection-model gaps found.
- [x] 2026-04-30 — SM-5 (Веха 6.5): rolling window transparency для `manager_daily`. Readiness/result теперь явно выносит `window_days_used`, `window_start`, `window_end` (из уже существующего `effective_period`) на top-level structured result. Service note заменил техническую формулировку про скользящее окно на manager-facing текст только при окне > 1 дня: «Список звонков ниже — только за выбранный день. Коучинговый разбор собран по базе за N рабочих дня: с … по …». При окне 1 день лишняя фраза не появляется. `call_list` для `СПИСОК ЗВОНКОВ ДНЯ` в expanded-window сценарии фильтруется по выбранному операционному дню из `filters`, при этом coaching/readiness blocks продолжают строиться из выбранного coaching window. Thresholds, analyzer contract, coaching logic, meaningful classification, service note funnel counters и `rop_weekly` не менялись. Verification: targeted SM-4/SM-5 tests — 7 passed; full regression `tests/test_manual_reporting.py tests/test_ai_provider_routing.py` — 97 passed.
- [x] 2026-04-30 — SM-4 (Веха 6.5): service note / funnel counters пересобраны на selection_model. `_build_manager_daily_selection_note()` в `report_templates.py` переработана: теперь читает `payload["selection_model"]` (raw_calls_total, meaningful_calls_total, included_in_report_total, exclusion_reasons) для построения честной воронки дня «Найдено в телефонии · содержательных · вошло в разбор». `meaningful_calls_total` показывается только если отличается от raw (иначе строка не дублируется). Причины исключения конвертируются в manager-facing labels через `_EXCLUSION_REASON_LABELS` (no raw codes exposed). `signal_report` префикс «Сигнальный отчёт ·» сохранён; `full_report` показывает ту же воронку без этого префикса. Legacy fallback path сохранён для payload без selection_model. Обновлён тест-монкипатч в `_build_manager_daily_group_result` — теперь пробрасывает `window_artifacts`. Verification: 76 тестов пройдено (72 existing + 4 новых SM-4 acceptance tests). Следующий шаг — SM-5: rolling window transparency.
- [x] 2026-04-30 — SM-3 (Веха 6.5): СПИСОК ЗВОНКОВ ДНЯ переключён на `meaningful_calls`. Добавлена функция `_build_meaningful_call_list(window_artifacts)` — фильтрует window_artifacts через `_classify_meaningful_call`, исключает beep/IVR/no-speech, включает sales и meaningful support/service calls, сортирует по времени. `build_manager_daily_payload` теперь строит `call_list` из `meaningful_calls` (через `window_artifacts`), а все coaching blocks (`analysis_worked`, `analysis_improve`, `kpi_overview.calls_count`, stage scores и т.д.) продолжают работать от `artifacts` (coaching_core/usable). Если `window_artifacts=None`, поведение идентично прежнему — `call_list = meaningful из artifacts`. Никаких изменений renderer, coaching blocks, service note, rolling window, readiness thresholds. Verification: 72 теста пройдено (67 existing + 5 новых SM-3 acceptance tests). Следующий шаг — SM-4: service note перестроить на counters из selection_model.
- [x] 2026-04-30 — SM-2 (Веха 6.5): meaningful-calls layer — bounded deterministic rule set. Добавлена константа `MEANINGFUL_ABSOLUTE_MIN_DURATION_SEC = 15` и функция `_classify_meaningful_call(artifact) -> tuple[bool, str | None]` с 5-правильным bounded rule set: R1 нет duration + нет transcript → `too_short_or_no_speech`; R2 duration < 15с + нет transcript → `too_short_or_no_speech`; R3 есть transcript → meaningful (real conversation, любой call_type); R4 `call_type=other` + `analysis_eligibility=not_eligible` + нет transcript → `ivr_or_autoanswer`; R5/default → meaningful. `_build_selection_model_counters` переработана: реальный `meaningful_calls_total` вместо proxy; `too_short_or_no_speech` и `ivr_or_autoanswer` заполнены из классификации; `_sm1_notes` proxy-ключ удалён. Support/internal с transcript остаётся meaningful (canonical doc F.1). Никаких изменений renderer, список звонков, service note не затронуты. Verification: 67 тестов пройдено (включая 8 новых SM-2 acceptance tests в обоих файлах). Следующий шаг — SM-3: СПИСОК ЗВОНКОВ ДНЯ переключить на meaningful_calls.
- [x] 2026-04-30 — SM-1 (Веха 6.5): payload/model changes for `manager_daily` selection model contract. Добавлена функция `_build_selection_model_counters(window_artifacts, usable_artifacts)` → вычисляет 6 счётчиков (`raw_calls_total`, `meaningful_calls_total`, `service_calls_total`, `coaching_candidate_calls_total`, `analyzed_calls_total`, `included_in_report_total`) и 5 exclusion reason codes (`too_short_or_no_speech`, `ivr_or_autoanswer`, `support_internal`, `not_enough_analysis`, `not_selected_for_core_review`). В `build_manager_daily_payload` добавлен новый параметр `window_artifacts` и ключ `selection_model` в payload. `_build_payload` и `_build_manager_daily_group_result` / `_build_single_report_result` теперь передают `window_artifacts` через цепочку вызовов. Никаких изменений renderer, список звонков, coaching-блоки и service note не затронуты. Verification: 78 тестов пройдено (55 existing + 4 новых SM-1 acceptance tests). SM-1 proxy: `meaningful_calls_total = raw_calls_total` (SM-2 добавит beep/IVR detection). Следующий шаг — SM-2: meaningful-calls layer.
- [x] 2026-04-30 — doc-only: зафиксирован canonical selection model и report contract для `manager_daily` (Веха 6.5, step 6.5 roadmap). Создан `docs/MANAGER_DAILY_SELECTION_MODEL.md` — source of truth по трём слоям данных (`raw_calls` / `meaningful_calls` / `coaching_core`), rolling window rule, counters/exclusion reasons и open questions. Добавлен ADR-048 в `docs/DECISIONS.md`. Обновлены `docs/CONTEXT_INDEX.md` (новая точка входа для selection model), `docs/MANUAL_REPORTING_PILOT.md` (синхронизирован с новым rule set), `docs/BUSINESS_READY_REPORT_PACK_TASKS.md` (добавлен раздел Selection Model Implementation — 6 bounded tasks SM-1…SM-6). Код не изменялся. Следующий шаг — bounded implementation tasks SM-1…SM-6 по одному.
- [x] 2026-04-29 v2 — manager-facing gaps fix (4 issues from Эльмира 27-04 retest): (1) **Service note funnel**: `_build_manager_daily_group_result` теперь добавляет `total_group_calls = len(artifacts)` (все звонки менеджера до оконного фильтра) в readiness dict; `_build_manager_daily_selection_note` показывает полную воронку "Найдено в телефонии: {total} · за отчётный день / в окне N дн.: {found} · с готовым разбором: {ready} · вошло в отчёт: {in_report}" вместо ошибочного дублирования одного числа. (2) **СИТУАЦИЯ ДНЯ signal→stage fix**: `_build_situation_body()` больше не утверждает, что сигнал "проявился на этапе X" — теперь сигнал и этап показаны раздельно: "Главный сигнал дня: {title}. Приоритетный этап: «{stage}» — {score}/5." (3) **ДОПОЛНИТЕЛЬНЫЕ 3 СИТУАЦИИ empty-state**: JS filter в `dataFromBundle()` теперь исключает items с `signal === 0` — `(item.signal || 0) > 0 && (item.title || item.client_said || item.how_to)`; ситуации без реального сигнала не рендерятся как пустые карточки. (4) **Typography cleanup visible**: SZ dict расширен с `{ h1:38, h2:26, body:22, meta:18 }` до `{ h1:38, h2:26, accent:24, body:22, cell:20, meta:18, caption:16 }`; все оставшиеся raw number literals (headCell, KPI-caption, DopSituatsii card, morning card, СИТУАЦИЯ ДНЯ title, ЧЕЛЛЕНДЖ, table cells, footer) заменены на SZ-символы — ни одного raw size literal в text run/cell calls не осталось. Scope: 3 файла (reporting.py, report_templates.py, generate_docx_report.js). Тесты (55) не нарушены — все assertions совместимы с новым форматом note (total_group_calls=0 → fallback к relevant_calls).
- [x] 2026-04-29 — manager-facing clarity + visual cleanup after live multi-day testing (Веха 6.5, bounded step): 4 класса дефектов устранены без изменения analyzer contract, readiness thresholds или scheduler. (1) **Прозрачная service note**: `_build_manager_daily_selection_note()` расширена на `full_report` и `signal_report`; формат заменён с "Это сигнальный отчёт: найдено X, вошло Y" на трёхчастную строку "Найдено в телефонии: X · с готовым разбором: Y · вошло в отчёт: Z + Not_voshlo + rolling window note". (2) **Unified empty-state**: `ДОПОЛНИТЕЛЬНЫЕ 3 СИТУАЦИИ` в JS-генераторе исправлен корень бага — `dataFromBundle()` читал `payload.additional_situations.situations` (сырой payload без fallback-контента), теперь читает `sections.additional_situations.situations` (обработанная секция с `client_said/meant/how_to/why` через `_build_v5_additional_situations_section()`). Все пустые situations с пустым title и client_said теперь отфильтровываются. (3) **Логический баг `СИТУАЦИЯ ДНЯ`**: `_priority_stage_row()` теперь fallback к минимально-скорящему этапу когда ни один явно не помечен `is_priority=True` — вместо "приоритетный этап не определён" блок показывает реальный этап из таблицы. (4) **ISO timestamps**: добавлены `_format_deadline_human()` в `report_templates.py` и `_format_iso_deadline()` в `reporting.py`; все deadline-поля в ПОЗВОНИ ЗАВТРА, СПИСОК ЗВОНКОВ, УТРЕННЯЯ КАРТОЧКА конвертируются из `2026-04-29T11:00:00+00:00` в `29 апр 11:00`. Дополнительно (5) **Typography cleanup**: в JS-генераторе введён `SZ = { h1:38, h2:26, body:22, meta:18 }` вместо 9+ разных raw-значений; удалён `COLORS.red` из inline-использований; добавлен `metaPara()` helper для gray/meta текста. Scope: 6 файлов изменено (report_templates.py, reporting.py, generate_docx_report.js, tests × 2 — обновлены assertions под новый формат note). Verification: `55 passed`; JS smoke: rich bundle → 19.5 KB OK, empty_state bundle → 12.4 KB OK.
- [x] 2026-04-24 — manager-facing polish for `signal_report` (Веха 6.5, bounded step: signal report assembly / wording / block behavior): без изменения analyzer contract, readiness thresholds, scheduler или `rop_weekly` выполнена редакторская доводка manager-facing signal report. Исправления: `readiness` теперь добавляется в payload до render, поэтому renderer/docx-first path видит `signal_report` context; в шапке сигнального отчёта добавлен compact business-facing note о найденных звонках, вошедшей подвыборке и причине исключения остальных; empty sections в HTML/PDF/docx-first больше не рисуют пустые таблицы, а показывают один короткий комментарий; `СИТУАЦИЯ ДНЯ` берёт stage label и score из одного render-ready priority stage row на шкале 0–5 и больше не смешивает stage name с raw score; intro/client/task/example wording сделаны компактнее, чтобы не повторять одну и ту же мысль; `ГОЛОС КЛИЕНТА` системно группирует повторяющиеся цитаты по coaching intent в один pattern с несколькими supporting quotes; `ПОЗВОНИ ЗАВТРА` заменён с ambiguous `Контекст` / `Скрипт открытия` на manager-facing `Срок/повод` / `Цель звонка` / `Первая фраза`; статус `Договор` переименован в `Договорённость` в docx/runtime labels. Verification: `docker compose exec api python -m pytest -q tests/test_manual_reporting.py tests/test_ai_provider_routing.py` -> `74 passed`; ordinary ready-only live run на `2026-04-06` подтвердил, что текущая persisted база всё ещё `skip_accumulate` (`49 selected`, `10 ready`, coverage `20.4%`), поэтому для bounded visual/content verification собран controlled docx-first signal artifact `signal_report_polish_check.pdf` (`render_variant=template_docx_first_pdf_manager_daily_template_v2`, `conversion_status=converted`, `page_count=3`, `size=97074`), где проверены selection note, новые labels, grouped voice pattern и `Договорённость`.
- [x] 2026-04-24 — analyzer enrichment for real `manager_daily` runs (Веха 6.5, bounded step: analyzer / prompt / validation enrichment): подтверждён gap, из-за которого shape-valid LLM-2 output для eligible sales calls мог проходить до semantic validation с `score_by_stage`, но без report-ready content shape (`strengths/gaps/recommendations/evidence_fragments`), а полностью пустые sales-relevant outputs отбрасывались как `semantically_empty_analysis`. Prompt `analyze.md` усилен bounded правилами: eligible sales call не должен возвращать coaching-empty shell; при подтверждённой зоне роста нужен минимум один `gap`, при позитивном сигнале — минимум один `strength`, при gap — usable `recommendation`, evidence fragments должны быть source-backed, а support/internal/non-coachable calls должны явно маркироваться `analysis_eligibility=not_eligible` вместо псевдо-анализа. В analyzer normalization добавлен bounded repair `max_score` из approved checklist before strict validation, retry prompt стал различать semantic-empty и `max_score` defects, а для eligible sales contracts добавлено deterministic enrichment из уже имеющихся criterion-level `score/comment/evidence` без выдумывания фактов. Для non-coachable/support semantic-empty cases добавлена явная forensic маркировка `not_coachable_or_reportable`. Verification: `docker compose exec api python -m pytest -q tests/test_ai_provider_routing.py tests/test_manual_reporting.py` -> `73 passed`; bounded real analyzer-only check на прежнем empty/support case `c71662bc-fca5-4eaf-b3db-435a0c3480ee` теперь возвращает `not_coachable_or_reportable` (`call_type=support`, `analysis_eligibility=not_eligible`); bounded real analyzer-only check на eligible sales case `26c151ce-c3ce-4b8b-a1ba-ed481427cc1e` вернул ready contract с `strengths=1`, `gaps=1`, `recommendations=3`, `evidence_fragments=6`, `score_by_stage=3`. Scope intentionally excludes report format, renderer, readiness threshold and scheduler changes.
- [x] 2026-04-24 — skip_accumulate empty-state fix: устранён класс дефектов, когда preview-shell payload (readiness=skip_accumulate) проходил через docx-first pipeline и давал NaN-значения, диагностический текст и пустые блоки в доставленном менеджеру PDF. Root cause — два источника: (1) `_build_manager_daily_empty_state_payload()` в `reporting.py` писал `refusal_count = "20.4%"` (строка вместо числа), `agreed_count = relevant_calls` (49 из многодневного окна), а `key_problem_of_day.description` содержал "Readiness outcome: skip_accumulate. Coverage 20.4%..." с внутренними диагностическими кодами; (2) `generate_docx_report.js` не проверял `payload.empty_state.enabled` и маппил строки через `Number()` без защиты. Исправления: в `reporting.py` `call_outcomes_summary` в empty_state payload теперь всегда содержит числовые нули (`agreed_count=0`, `rescheduled_count=0`, `refusal_count=0`, `open_count=0`, `tech_service_count=0`), `key_problem_of_day.description` — бизнес-нейтральный текст без internal codes; в `generate_docx_report.js` добавлены `safeNumber()` helper (защищает от `Number("Нет базы")` = NaN, `Number("20.4%")` = NaN) и `emptyStateData()` — early return с чистыми плейсхолдерами при `payload.empty_state.enabled === true`. Scope: 2 файла, 0 новых, 0 архитектурных изменений. Verification: `54 passed`; JS smoke test: empty_state bundle → clean 12.7 KB docx без NaN и диагностического текста; rich bundle → 19.5 KB docx без регрессий. Note: если реальные звонки по-прежнему не проходят readiness порог, docx/PDF будет корректно показывать "Данных недостаточно" — это ожидаемое поведение, не баг формата.
- [x] 2026-04-23 — `manager_daily` switched to docx-first PDF delivery (Веха 6.5, roadmap step: перевести `manager_daily` delivery на `docx-first pipeline` с конвертацией в PDF перед отправкой): canonical format source of truth зафиксирован как approved good artifact `Ежедневный_отчет_Кешубаева_2026-04-06_v5.docx` и его generator `scripts/generate_docx_report.js`. Ordinary runtime `manager_daily` больше не считает отдельный Python PDF renderer основным delivery source: для `manager_daily_template_v2` runtime теперь сначала строит канонический `.docx`, затем конвертирует его в `.pdf` через headless LibreOffice path `soffice --headless --convert-to pdf`, и уже этот PDF уходит в Telegram / delivery channel. Для runtime environment подготовлен deterministic conversion stack: `nodejs + npm + global docx + libreoffice-writer`, а repo-level `scripts/` смонтированы в Python containers как read-only source of truth path. Bounded safe fallback сохранён и сделан прозрачным: если docx generation или conversion падает, pipeline не рушится бесконтрольно, а возвращается к existing runtime PDF path с явным `conversion_status=fallback_runtime_pdf` и `conversion_error` в artifact metadata / diagnostics / observability. Delivery observability расширена: теперь structured result и channel logs явно показывают `generator_path`, `source_of_truth_generator_path`, `conversion_path`, `artifact_type`, `conversion_status`. Regression coverage обновлён тестом на `prefer_docx_first=True`; verification: `docker compose -f /root/ai-sales-analyzer/docker-compose.yml exec api pytest -q tests/test_manual_reporting.py` -> `54 passed`. Live ordinary verification через `POST https://localhost:8444/pipeline/calls/report-run` на `preset=manager_daily`, `department=472cda28-ce71-494c-9068-25d3ffbf7399`, `manager=09cae83f-7ac1-4ee0-b1d5-3a76c8053c3f`, `extension=322`, requested period `2026-04-06`, `send_email=false` подтвердил новый delivery path: `artifact.render_variant=template_docx_first_pdf_manager_daily_template_v2`, `artifact.build_path=docx_first_pdf_delivery`, `artifact.source_artifact.kind=docx_report`, `artifact.conversion_path=soffice --headless --convert-to pdf`, `artifact.conversion_status=converted`, `delivery.transport.telegram_test_delivery.status=delivered`. Конкретный live case остался `skip_accumulate` / preview-shell по readiness thresholds, но сам delivered PDF уже был построен через canonical `docx -> pdf` conversion, а не через legacy standalone runtime PDF renderer.
- [x] 2026-04-23 — `manager_daily` same-payload parity task completed (Веха 6.5, roadmap step: закрыть разрыв между эталонным docx и runtime PDF через same-payload parity): canonical verification case зафиксирован как `manager=Эльмира Кешубаева`, `department=472cda28-ce71-494c-9068-25d3ffbf7399`, `date=2026-04-06`, `selected_calls=8` synthetic verification calls from `verification_only_v0`. Новый canonical repo payload source добавлен как generated repo bundle `verification_manager_daily_v5_case_bundle.json`; он содержит один и тот же `case + payload + runtime report model`, от которого теперь строятся и rich runtime PDF, и standalone docx. `core/app/agents/calls/verification_report_runner.py` теперь умеет собирать этот canonical bundle, писать его в repo, сохранять runtime PDF и отправлять parity-check PDF в test Telegram без preview-shell и без AI/DB reuse drift. `scripts/generate_docx_report.js` больше не держит hardcoded v5 data вручную, а читает тот же verification bundle, поэтому docx и PDF реально собираются из одного и того же verification payload. В runtime helper layer добавлены bounded parity hooks: `report_templates.py` теперь поддерживает verification-only rich overrides для `day_score`, `money_on_table`, `call_breakdown.rows`, `voice_of_customer.interpretation` и expanded `additional_situations`, чтобы PDF повторял good docx по структуре и presentation logic, не меняя analyzer contract. Дополнительно устранён time-source drift в verification case: runtime теперь показывает local call times `09:15 / 10:30 / ...`, а не UTC-shifted `03:15 / 04:30 / ...`. Verification: `docker compose -f /root/ai-sales-analyzer/docker-compose.yml exec api pytest -q tests/test_manual_reporting.py` -> `53 passed`; rich same-payload runtime artifact: `verification_Эльмира_Кешубаева_2026-04-06_runtime.pdf`, `page_count=7`, no preview-shell markers; Telegram parity-check delivery подтверждён через verification runner. Residual differences между docx и PDF после same-payload alignment остались только несущественные: Word-vs-PDF typography/pagination engine и неидентичная графика footer/title-page, но не состав блоков, не порядок и не данные.
- [x] 2026-04-23 — `manager_daily` ordinary runtime PDF brought to exact approved v5 structure (Веха 6.5, roadmap step: довести ordinary runtime PDF pipeline до точного соответствия утверждённому docx/v5-формату): канонический source of truth подтверждён как standalone generator `scripts/generate_docx_report.js` плюс repo artifact `Ежедневный_отчет_Кешубаева_2026-04-06_v5.docx`. Найденный root cause: ordinary runtime уже собирал правильные 13 секций в data/html/text, но PDF path всё ещё шёл через generic `_render_pdf_report()` и отдавал упрощённый двухстраничный text-PDF вместо v5 composition. Исправление: `core/app/agents/calls/report_templates.py` снова использует dedicated `manager_daily` HTML/PDF render path только для `manager_daily_template_v2`; новый runtime PDF собирает те же 13 блоков в том же порядке, что и docx/v5 source of truth: ШАПКА / СВОДНАЯ ТАБЛИЦА ЗВОНКОВ / ДЕНЬГИ НА СТОЛЕ / PIPELINE ТЁПЛЫХ ЛИДОВ / БАЛЛЫ ПО ЭТАПАМ / СИТУАЦИЯ ДНЯ / РАЗБОР ЗВОНКА / ГОЛОС КЛИЕНТА / ДОПОЛНИТЕЛЬНЫЕ 3 СИТУАЦИИ / ЧЕЛЛЕНДЖ НА ЗАВТРА / ПОЗВОНИ ЗАВТРА / СПИСОК ВСЕХ ЗВОНКОВ ДНЯ / УТРЕННЯЯ КАРТОЧКА. Safe fallback сохранён без format drift: при пустых данных блоки остаются на месте и заполняются structural placeholders, а не исчезают и не заменяются старым layout. Regression coverage усилен: `tests/test_manual_reporting.py` и `core/tests/test_manual_reporting.py` теперь требуют `page_count >= 6` и проверяют сохранение точного порядка 13 v5 labels в HTML/runtime output. Live ordinary runtime verification через обычный `POST https://localhost:8444/pipeline/calls/report-run` на case `department=472cda28-ce71-494c-9068-25d3ffbf7399`, `manager=09cae83f-7ac1-4ee0-b1d5-3a76c8053c3f`, `extension=322`, requested period `2026-04-06`, `preset=manager_daily`, `mode=report_from_ready_data_only`, `send_email=false` подтвердил exact runtime path: `artifact.filename=manager_daily_preview_Эльмира Кешубаева_2026-04-02_manager_daily_template_v2.pdf`, `artifact.page_count=7`, `diagnostics.report_origin.generator_path=app.agents.calls.report_templates.render_report_artifact`, `diagnostics.report_origin.template_version=manager_daily_template_v2`, `delivery.transport.telegram_test_delivery.status=delivered`. GitHub state rechecked against remote: `origin/main` = `5bb0e60e049593d00a3507067b7c172fda402d3e` before this exact-alignment patch.
- [x] 2026-04-22 — Runtime canonical switch to `manager_daily_template_v2` (Веха 6.5, bounded integration): root cause рассинхрона подтверждён кодом и runtime evidence. Обычный `/pipeline/calls/report-run` брал active template только из `core/app/agents/calls/report_template_assets/active_versions.json`, где `manager_daily` всё ещё был закреплён как `manager_daily_template_v1`, тогда как улучшенный “good report” существовал вне canonical runtime selection как отдельный verification / standalone path. Исправление: добавлен versioned asset slice `core/app/agents/calls/report_template_assets/manager_daily/manager_daily_template_v2/`, active registry переключён на `v2`, legacy `manager_daily_template_v1` оставлен отдельно. В structured result / observability / diagnostics / artifact metadata добавлены явные provenance-поля: `template_version`, `template_id`, `render_variant`, `generator_path`. Safe verification: `python -m pytest -q tests/test_manual_reporting.py` внутри `api` контейнера — 52/52 passed. Обычный safe runtime run через реальный `POST https://localhost:8444/pipeline/calls/report-run` с `preset=manager_daily`, `mode=report_from_ready_data_only`, `department=472cda28-ce71-494c-9068-25d3ffbf7399`, `manager=09cae83f-7ac1-4ee0-b1d5-3a76c8053c3f`, `extension=322`, requested period `2026-04-06`, `send_email=false` подтвердил normal pipeline path: итоговый artifact `manager_daily_preview_Эльмира Кешубаева_2026-04-02_manager_daily_template_v2.pdf`, `page_count=6`, `telegram_test_delivery.status=delivered`, `observability.summary.template_version=manager_daily_template_v2`, `diagnostics.report_origin.generator_path=app.agents.calls.report_templates.render_report_artifact`. Run остался `skip_accumulate`/preview-shell из-за ready coverage 20.4% и без новых billable AI шагов — это expected safe-mode outcome, а не regress в template switch.
- [x] 2026-04-22 — Standalone .docx generator (Веха 6.5, Task 4): Реализован Node.js скрипт `scripts/generate_docx_report.js` с использованием `docx` npm library. Генерирует `Ежедневный_отчет_Кешубаева_2026-04-06_v5.docx` (19.9 KB) в структуре эталонного шаблона `Ежедневный_отчет_v4_ФИНАЛ.pdf`. Ровно 13 блоков в порядке ТЗ: Шапка / Сводная таблица / Деньги на столе / Pipeline / Баллы по этапам / Ситуация дня / Разбор звонка / Голос клиента / Дополнительные 3 ситуации / Челлендж / Позвони завтра / Список звонков / Утренняя карточка. Ключевые преобразования: (1) Шкала 0–10 → 0–5 для всех баллов (÷2); (2) Прогресс-бары █/░ длиной 20, формула round(score/5×20); (3) Добавлены новые блоки ДЕНЬГИ НА СТОЛЕ и PIPELINE (отсутствовали в текущем PDF); (4) СИТУАЦИЯ ДНЯ расширена — добавлены «Что хотел клиент», «Наша задача», 3 речёвки, «Почему работает»; (5) ГОЛОС КЛИЕНТА — добавлен 3-й столбец «Смысл → Как ответить»; (6) ПОЗВОНИ ЗАВТРА — добавлены столбцы «Приоритет» 🔴🟡🔵 и «Скрипт открытия»; (7) РАЗБОР ЗВОНКА — добавлен столбец «Момент» с временными метками; (8) ДОПОЛНИТЕЛЬНЫЕ СИТУАЦИИ — расширены до 4-строчной структуры по эталону; (9) УТРЕННЯЯ КАРТОЧКА — добавлена финансовая строка + челлендж; (10) Удалены блоки КЛЮЧЕВАЯ ПРОБЛЕМА / РЕКОМЕНДАЦИИ / ДИНАМИКА. Footer «Конфиденциально · Только для менеджера и РОПа» на всех страницах кроме первой (titlePage: true). Структурная валидация: valid OOXML ZIP, 26 XML members, document.xml 206 KB, все 13 блоков подтверждены. Soffice недоступен в среде — PDF-конвертация не выполнена. Файл отправлен в test Telegram.
- [x] 2026-04-21 — Visual/layout hardening pass `manager_daily_template_v1` (Веха 6.5): Устранены 4 класса дефектов перекрытия, выявленных по verification PDF. (1) Page 1 — `executive_narrative` body/progress теперь измеряются динамически (`measure_height`), executive block высота расширяется под контент; signal card следует за exec block bottom (не фиксированный top=362). (2) Page 1 — СИТУАЦИЯ ДНЯ также следует за signal card; block height увеличен до 116px (was 96) при наличии call_example — скрипты больше не обрезаются section bar БАЛЛЫ. (3) Page 1 — `_problem_card_top` теперь всегда вычисляется динамически после окончания stage rows loop (`_st_top + 6`); добавлен overflow guard в loop (`_max_st_row_end = height - 36 - 90 - 6`) — таблица любой длины не пишет поверх КЛЮЧЕВАЯ ПРОБЛЕМА. (4) Page 2 — rec card heights динамические: `_ex_box_h` (min 44px) растёт под longer before/after text; `_why_top_offset` вычисляется от фактического дна example boxes — «Почему это работает» больше не перекрывает «Как лучше». (5) Page 4 — шаг `_item_top += 30 → 44` для двухколонного layout «Что сработало / Что исправить» — interpretation строки не наезжают на следующий item. PDF (848KB) отправлен в test Telegram.
- [x] 2026-04-21 — Format verification backfill v2 (Веха 6.5): Исправлен контракт fixture-данных, все блоки второй корзины реально наполнены и подтверждены содержательно. Исправления: `gaps`/`strengths` переведены с `"label"` на `"title"` (ключ, который читает `_aggregate_finding_items`); `recommendations` переведены на `"criterion_name"` + `"better_phrase"` (контракт `_aggregate_recommendation_cards`); `evidence_fragments` теперь содержат `"client_text"` ≥ 15 символов (для ГОЛОС КЛИЕНТА); статусы `rescheduled`/`refusal`/`open` корректно генерируются через `next_step_fixed=False` + `reason_not_fixed` с ключевыми словами «перезвон»/«отказ» (логика `_derive_call_status_and_deadline`). Итоговые показатели блоков: СИТУАЦИЯ ДНЯ — title=«Не выявляет детальные потребности…», call_example найден (worst-score call); РАЗБОР ЗВОНКА — не placeholder, 4 этапа; ГОЛОС КЛИЕНТА — 3 цитаты; ДОПОЛНИТЕЛЬНЫЕ СИТУАЦИИ — 3 ситуации; ПОЗВОНИ ЗАВТРА — 5 контактов (2 rescheduled + 3 agreed); БАЛЛЫ ПО ЭТАПАМ — 4 этапа, приоритетный «Выявление потребностей» avg=1.9 с 4 детализированными критериями. Скрипты в СИТУАЦИИ ДНЯ вычисляются на стороне рендерера из `better_phrasing` рекомендаций. PDF (848KB) отправлен в test Telegram. Isolaton: нет DB-записи, `instruction_version="verification_only_v0"` исключает артефакты из production reuse path.
- [x] 2026-04-20 — БАЛЛЫ ПО ЭТАПАМ: детализация критериев (Веха 6.5 вторая корзина): В блоке БАЛЛЫ ПО ЭТАПАМ добавлена компактная criterion-level детализация для приоритетного этапа (первый этап < 4.0 по воронке). Правило: агрегируются `criteria_results` из `scores_detail.score_by_stage` всех артефактов; для каждого критерия вычисляется avg `score/max_score*10` (0–10 шкала); результаты сортируются по score asc; слабые критерии (`< 5.0`) выделяются. HTML: строка критериев вставляется сразу после приоритетного этапа как `<tr class="stage-criteria-row">` с chip-плашками (`crit-weak` / `crit-ok`). PDF: приоритетный ряд расширяется с 18 до 28px, вторая строка показывает до 3 слабых критериев с баллами. `_problem_card_top` динамический (сдвиг +10px при наличии criteria_detail). Safe fallback: если `criteria_results` нет — ряд не расширяется, поведение идентично прежнему. Изменения: `reporting.py` (`_aggregate_stage_scores` расширен — агрегация `crit_stage`/`crit_names`, `criteria_detail` в stage row), `report_templates.py` (`_build_stage_score_rows` — добавлен `criteria_detail` passthrough; HTML — цикл вместо comprehension для criteria sub-row; PDF — `_stage_rows` назначен до layout-расчётов, `_has_prio_crit`, dynamic `_problem_card_top`, expanded priority row), `layout.css` (`.stage-criteria-row`, `.stage-criteria`, `.crit-chip`, `.crit-weak`, `.crit-ok`, `.crit-score`). Верификация: 48/48 тестов.
- [x] 2026-04-20 — СПИСОК ЗВОНКОВ: колонка Контекст (Веха 6.5 вторая корзина): В таблицу СПИСОК ЗВОНКОВ добавлена 7-я колонка «Контекст» (между «Статус» и «Следующий шаг»). Детерминированное правило: agreed+deadline → «до {deadline}»; rescheduled+deadline → «→ {deadline}»; rescheduled без даты → «перезвон»; open/refusal + reason_not_fixed → первые 28 символов причины; иначе → «—». Данные: `follow_up.due_date_text/due_date_iso` (уже в call row как `deadline`) + новое поле `reason` из `follow_up.reason_not_fixed` добавлено в `_build_daily_call_row`. Изменения: `reporting.py` (добавлен `reason` в call row), `report_templates.py` (новый `_call_context_label()` helper; `columns` 6→7, `rows` 6→7 элементов с Контекст между Статус и Следующий шаг; HTML fallback colspan 6→7; PDF col_widths 6→7, col_widths скорректированы под 7 колонок, status index 4 без изменений). Верификация: 48/48 тестов.
- [x] 2026-04-20 — ПОЗВОНИ ЗАВТРА (Веха 6.5 вторая корзина): Добавлен блок ПОЗВОНИ ЗАВТРА на страницу 5 PDF/HTML (после ДОПОЛНИТЕЛЬНЫЕ СИТУАЦИИ). Shortlist до 5 follow-up контактов, собираемый детерминированно из `follow_up` данных артефактов: приоритет — rescheduled (перезвон) → agreed (договорённость) → open (открытый); исключены refusal, support, internal; дедупликация по client_label; сортировка по дедлайну (soonest first). По каждому контакту — короткий manager-facing opening script: rescheduled+deadline «Звоню, как договорились ({deadline}).»; rescheduled без дедлайна «Продолжаем наш разговор»; agreed+next_step «Звоню уточнить детали: {step}»; agreed «Готов двигаться дальше»; open/cold «Хотел(а) подвести итог нашего разговора». Safe fallback: `is_placeholder=True` или пустые контакты → «Нет открытых контактов». Изменения: `reporting.py` (`_call_tomorrow_opening_script` helper, новый `_build_call_tomorrow`, добавлен в payload как `call_tomorrow`), `report_templates.py` (model builder — секция `call_tomorrow`; HTML — `_render_call_tomorrow_html()` helper, добавлен в `page_five` после additional_situations; PDF — блок ПОЗВОНИ ЗАВТРА на `page5` с cursor tracking от `_ct_top`), `semantic.json` (секция `call_tomorrow`), `layout.css` (`.call-tomorrow`, `.ct-row`, `.ct-badge`, `.ct-script` + дочерние стили). Верификация: 48/48 тестов.
- [x] 2026-04-20 — ДОПОЛНИТЕЛЬНЫЕ СИТУАЦИИ (Веха 6.5 вторая корзина): Добавлен блок ДОПОЛНИТЕЛЬНЫЕ СИТУАЦИИ как страница 5 PDF/HTML (morning card сдвинут на страницу 6). До 3 coaching-ситуаций из существующих данных: secondary gaps (`improve_items[1:]`, исключая top gap = СИТУАЦИЯ ДНЯ) и strengths (`worked_items`). Каждая ситуация: вид (gap → «Зона роста» / strength → «Сильная сторона»), title, signal count, interpretation. Дедупликация: `top_gap_title` не дублируется. Safe fallback: `is_placeholder=True` → нейтральный текст. Изменения: `reporting.py` (новый `_build_additional_situations`, добавлен в payload как `additional_situations`), `report_templates.py` (model builder — секция `additional_situations`; HTML — `_render_additional_situations_html()` helper, новая `page_five`, morning card → `page_six`, footer 5→6; PDF — новый `page5` с карточками gap/strength, morning card → `page6`, footer 5→6), `semantic.json` (секция `additional_situations`), `layout.css` (`.add-situations` + дочерние стили). Верификация: 48/48 тестов.
- [x] 2026-04-17 — ГОЛОС КЛИЕНТА (Веха 6.5 вторая корзина): В блоке РАЗБОР ЗВОНКА (страница 4 PDF / HTML) добавлен раздел ГОЛОС КЛИЕНТА с тремя клиентскими ситуациями. Правило выбора (детерминированное): из всех артефактов дня сначала берутся `evidence_fragments` типа `missed_opportunity` с `client_text` ≥ 15 символов, затем остальные `evidence_fragments`, затем `product_signals[].quote` как fallback. Дедупликация по тексту цитаты. Каждая ситуация: цитата клиента, имя+время, контекст (`why`/`topic`). Изменения: `reporting.py` (новый `_build_voice_of_customer`, добавлен в payload как `voice_of_customer`), `report_templates.py` (model builder — секция `voice_of_customer`; HTML — `_render_voice_of_customer_html()` helper, вызывается из `page_four`; PDF — ГОЛОС КЛИЕНТА добавлен в конец `page4` с cursor tracking после РАЗБОР ЗВОНКА), `semantic.json` (секция `voice_of_customer`), `layout.css` (`.voice-of-customer` + дочерние стили). Safe fallback: `is_placeholder=True` или пустые ситуации — показывает нейтральный текст. Верификация: 68/69 тестов.
- [x] 2026-04-17 — РАЗБОР ЗВОНКА (Веха 6.5 вторая корзина): Добавлен отдельный блок РАЗБОР ЗВОНКА как новая страница PDF (стр. 4) и HTML-страница. Показывает один конкретный звонок как обучающий пример: заголовок с именем/временем, поэтапная таблица с баллами (Этап / Балл / Оценка, слабые этапы выделены), 2-колоночный анализ «Что сработало» / «Что исправить», блок «Попробуй так» с конкретной речёвкой. Правило выбора: тот же что call_example — минимальный score среди артефактов с top gap label. Данные: `scores_detail.score_by_stage` + `strengths[:2]` + `gaps[:2]` + `recommendations[0]`. Изменения: `reporting.py` (новый `_build_call_breakdown`, добавлен в payload как `call_breakdown`), `report_templates.py` (model builder — секция `call_breakdown`; HTML — новая `page_four`, footer morning card 4→5; PDF — новый `page4`, footer morning card 4→5), `semantic.json` (секция `call_breakdown`), `layout.css` (`.call-breakdown` и все дочерние стили). Safe fallback: `is_placeholder=True` → блок рендерится с текстом «Недостаточно данных». Верификация: 68/69 тестов.
- [x] 2026-04-17 — Пример звонка в СИТУАЦИЯ ДНЯ (Веха 6.5 вторая корзина): В блоке СИТУАЦИЯ ДНЯ добавлен конкретный пример звонка с именем клиента, временем и короткой причиной. Правило выбора: среди звонков, у которых в `gaps` есть label = top gap, берётся звонок с наименьшим score (наиболее показательная иллюстрация проблемы). Данные: `contact_name`/`contact_phone` из `scores_detail.call`, `call_started_at` из ReportArtifact, `impact`/`comment`/`evidence` из gap-item (≤80 символов). Safe fallback: если matching artifacts нет — `call_example=None`, блок не рендерится. Изменения: `reporting.py` (новый helper `_build_problem_call_example`, `call_example` добавлен в `key_problem_of_day`), `report_templates.py` (новый helper `_render_situation_call_example_html`; `call_example` в модели; HTML: `<div class="situation-example">`; PDF: условное расширение блока 80→96 при наличии примера, cascade — `_stage_bar_top`, `_st_top_init`, `_problem_card_top` — все условные), `layout.css` (`.situation-example`, `.situation-example-label`, `.situation-example-contact`, `.situation-example-reason`). Верификация: 68/69 тестов.
- [x] 2026-04-17 — Count паттерна (Веха 6.5 вторая корзина): В блоке СИТУАЦИЯ ДНЯ добавлен количественный индикатор «Паттерн повторился в N из M звонков». Данные: `improve_items[0]["signal"]` (count звонков с top gap) и `calls_count` (всего звонков) — оба уже вычислялись в `build_manager_daily_payload`. Изменения: `reporting.py` (`_build_manager_daily_key_problem` получил параметр `calls_count`, добавляет `pattern_count` + `total_calls` в return dict; call site обновлён), `report_templates.py` (новый helper `_build_pattern_count_label`, `pattern_count_label` добавлен в model section; HTML: `<div class="situation-count">` между body и scripts; PDF: count line top=462 size=8.0 color=(100,110,150), scripts сдвинуты до 474/485/496 при наличии count), `layout.css` (`.situation-count` стиль). Safe fallback: если count=None или total=0, блок не рендерится. Верификация: 68/69 тестов (1 pre-existing rop_weekly failure).
- [x] 2026-04-17 — Речёвки (Веха 6.5 вторая корзина): В блоке СИТУАЦИЯ ДНЯ добавлены 3 micro-script речёвки. Источник: `recommendations[].better_phrasing` (уже LLM-сгенерированные из analyzer). Детерминированный fallback: `_STAGE_SCRIPT_FALLBACKS` (7 этапов × 3 скрипта) + `_STAGE_SCRIPT_GENERIC`. Логика: до 3 better_phrasing из cards (дедупликация), затем stage-specific fallbacks по `stage_code` приоритетного этапа, затем generic. Изменения: `report_templates.py` (добавлены `_STAGE_SCRIPT_FALLBACKS`, `_STAGE_SCRIPT_GENERIC`, `_build_situation_scripts()`; модельный builder использует `scripts` вместо `script_placeholder`; HTML: `<ol class="situation-scripts">` список; PDF: блок расширен h 64→80, 3 скрипта рисуются size=8.5 color=(70,70,130)), `layout.css` (`.situation-scripts`, `.situation-scripts li`). Верификация: 68/69 тестов прошли (1 pre-existing failure в rop_weekly не связан с речёвками).
- [x] 2026-04-17 — Task 6 (Веха 6.5): ПАМЯТКА убрана из финального manager_daily PDF и HTML. memo_legend секция сохранена в модели и text-рендере. PDF: page4 (ПАМЯТКА) удалена из pipeline, страниц стало 4; УТРЕННЯЯ КАРТОЧКА стала страницей 4. HTML: page_four удалена из join; footer перенумерован. Изменения: `report_templates.py` (HTML-рендер — удалены `memo_cards`, `page_four`, `memo` extraction; PDF-рендер — удалён блок page4, `memo` extraction, footer(page5) 5→4). Верификация: 48 тестов прошли.
- [x] 2026-04-17 — Task 5 (Веха 6.5): СПИСОК ЗВОНКОВ — перегруппировка колонок. Новые колонки: # / Время / Клиент / Тема / Статус / Следующий шаг. Убраны Длит. и Балл. Добавлена нумерация строк (#) по финальному списку. Колонка Тема собирается из `classification.call_type` + `classification.scenario_type` (короткие метки через `_call_topic_label()`). Изменения: `reporting.py` (`_build_daily_call_row` — добавлен `scenario_type` из classification), `report_templates.py` (новые константы `_CALL_TYPE_SHORT` / `_SCENARIO_TYPE_SHORT`, helper `_call_topic_label`, модельный builder — новые columns/rows, HTML рендер — статус index 3→4, PDF рендер — новые col_widths + статус index 3→4). Верификация: 48 тестов прошли.
- [x] 2026-04-17 — Task 4 (Веха 6.5): блок ГЛАВНЫЙ ФОКУС НА ЗАВТРА заменён на СИТУАЦИЯ ДНЯ — детерминированный структурный блок. Заголовок: «СИТУАЦИЯ ДНЯ · {Название этапа} ({балл}) — первый этап ниже 4 по воронке» (из `score_by_stage`). Основное описание: `key_problem_of_day.description`. Статический placeholder для речёвок (без LLM). Изменения: `report_templates.py` (helper `_build_situation_title`, секция `main_focus_for_tomorrow` → `situation_title`/`body`/`script_placeholder`, HTML рендер: `banner yellow` → `situation-day` блок, PDF рендер: yellow rect → синий accent block), `layout.css` (`.situation-day`, `.situation-title`, `.situation-body`, `.situation-script`), `semantic.json` (label → «СИТУАЦИЯ ДНЯ», kind → `situation_card`, убран `model_dependent`), `core/tests/test_manual_reporting.py` (обновлён assertion). Верификация: 48 тестов прошли.
- [x] 2026-04-16 — Task 3 (Веха 6.5): заменён блок РАЗБОР (2 колонки Что сработало / Над чем работать) на таблицу БАЛЛЫ ПО ЭТАПАМ с колонками Этап / Сегодня / Шкала / Приоритет. Правило приоритета: первый этап ниже 4.0 сверху по воронке (Э1→Э2→Э3→Э4→Э5→Э6→Сквозной) помечается как приоритетный. Visual bars — CSS proportional fill (HTML) + drawn rect bars (PDF). Данные: `score_by_stage` агрегируется из per-call `scores_detail` в `build_manager_daily_payload()` через `_aggregate_stage_scores()` (0–10 шкала = stage_score/max_stage_score*10). Изменения: `reporting.py` (`_STAGE_FUNNEL_ORDER`, `_aggregate_stage_scores`, `score_by_stage` добавлен в оба payload), `report_templates.py` (`_build_stage_score_rows`, `review_block` section → `stage_rows`, HTML рендер, PDF рендер), `layout.css` (stage table styles), `semantic.json` (label → «БАЛЛЫ ПО ЭТАПАМ», kind → `stage_scores_table`). Верификация: все 48 тестов прошли, детерминизм правила приоритета проверен.
- [x] 2026-04-16 — Task 2 (Веха 6.5): заменён short delivery wrapper и добавлена секция УТРЕННЯЯ КАРТОЧКА в PDF. Telegram caption теперь содержит business-facing morning card: приветствие по имени менеджера, итог дня (N звонков → договорённости / открытые), top-3 открытых звонка из `call_list`, фокус дня из `focus_of_week`. Секция УТРЕННЯЯ КАРТОЧКА добавлена последней страницей (page 5) PDF. Изменения: `delivery.py` (новый param `morning_card_text`, используется как caption), `reporting.py` + `scheduled_reporting.py` (передают `rendered.get("morning_card_text")`), `report_templates.py` (новый helper `_build_morning_card_data`, секция в модели, HTML page_five, PDF page5, `morning_card_text` в `render_report_artifact`), `layout.css` (`.morning-card`, `.morning-greeting`, `.morning-summary`, `.morning-open-list`, `.morning-challenge`), `semantic.json` (добавлен `morning_card` kind).
- [x] 2026-04-16 — Task 1 (Веха 6.5): заменён верхний summary block `manager_daily` — 5 тайлов (звонков/балл/% сильных/базовых/проблемных) заменены на горизонтальную outcome-таблицу с 6 колонками (ЗВОНКОВ / ДОГОВОРЕННОСТЬ / ПЕРЕНОС / ОТКАЗ / ОТКРЫТ / ТЕХ/СЕРВИС). Изменения: `reporting.py` (`_build_call_outcomes_summary` — добавлен `tech_service_count` через `call_type in {support, internal}`), `report_templates.py` (model builder, HTML renderer, PDF renderer, добавлен `_outcome_col_class`), `layout.css` (добавлены `.outcome-table`, `.outcome-cell`, `.outcome-value`, `.outcome-label`), `semantic.json` (kind `day_summary` → `outcome_table`). Все 48 passing tests прошли.
- [x] 2026-04-16 — Зафиксирован prioritized task breakdown для Вехи 6.5 `Business-ready Report Pack`: выполнено сравнение current repo версии `manager_daily` с новой версией отчёта (`Ежедневный_отчет_v4_ФИНАЛ.pdf`), все изменения разложены по трём корзинам, определены первые 6 implementation tasks; source of truth — `docs/BUSINESS_READY_REPORT_PACK_TASKS.md`
- [x] 2026-04-16 — Выполнен baseline review current report layer: зафиксированы active template versions (`manager_daily_template_v1`, `rop_weekly_template_v1`), key files, структура секций обоих отчётов и gaps относительно reference
- [x] 2026-04-16 — Для report work зафиксирована canonical three-bucket prioritization: `Делать сейчас` / `Делать вторым этапом` / `Делать в последнюю очередь`; source of truth для этого правила теперь `docs/REPORT_BACKLOG_PRIORITIZATION.md`
- [x] 2026-04-15 — Выполнен bounded UX-fix operator UI для pre-pilot работы: manual run и schedules разделены на две вкладки (`Ручной запуск` / `Расписание`), `Existing Schedules` теперь показывают department/manager labels вместо raw UUID как primary text, добавлено явное `Удалить` с bounded soft-delete semantics (schedule исчезает из active list и future due-scan, но historical batches/drafts сохраняются)
- [x] 2026-04-15 — Исправлен operator UI regression для `/pipeline/calls/report-ui`: confirmed root cause был в том, что `report-ui/context` падал при runtime-migration mismatch (`report_schedules` table missing), из-за чего frontend оставался с пустыми select fields; context builder теперь сохраняет static choices (`presets`, `modes`, bounded scheduled metadata) отдельно от DB-dependent schedule storage, UI показывает isolated schedule-storage warning вместо глобального empty state, а runtime schema доведена до актуального состояния через migration `d8b0f4c8d412` + restart `api/worker/beat`
- [x] 2026-04-15 — Закрыт внутренний technical hardening для `scheduled_reviewable_reporting`: добавлены server-side lifecycle transitions, due-occurrence idempotency guard, disabled/future schedule guards, backend edit-whitelist enforcement с audit trail (`original_generated_block`, `edited_block`, `editor`, `edited_at`), deterministic period-rule tests и architecture sync в `ARCHITECTURE.md`
- [x] 2026-04-15 — Реализован bounded pre-pilot block `scheduled_reviewable_reporting`: в existing backend/operator UI добавлены `Schedules`, automatic schedule -> draft lifecycle, review queue, bounded edit surface только для business-facing report blocks, manual approve before business delivery и docs sync под новый operating mode
- [x] 2026-04-15 — `Pilot Ready` docs доведены до bounded closure state without billing dependency: в repo зафиксированы pilot baseline version, pilot scope in/out, pilot KPI list, fixed pilot group placeholder, baseline metrics package, manual AI validation rule, delivery success rule, call-type segmentation rule, explicit external blocker separation и exact closure rerun procedure after user-confirmed top-up
- [x] 2026-04-14 — Repo docs синхронизированы под новый agreed порядок работ: `Pilot Ready -> Business-ready Report Pack -> Pilot Live -> Full Report Mechanism Upgrade`; убран статусный конфликт в `ROADMAP.md`, обновлены текущий фокус и next steps в `PROGRESS.md`, решение уже зафиксировано в `ADR-043`
- [x] Концепт продукта v2.0 (документ для руководства)
- [x] Архитектурные решения зафиксированы (ADR 001-005)
- [x] Структура проекта определена
- [x] Создана файловая структура и контекстные файлы
- [x] 2026-03-17 — Шаг 2: `config/settings.py` (Pydantic Settings, все переменные)
- [x] 2026-03-17 — Шаг 3: Схема БД (SQLAlchemy модели + Alembic + initial migration)
- [x] 2026-03-17 — Шаг 4: OnlinePBX intake (получение, фильтрация и сохранение звонков в `interactions`)
- [x] 2026-03-17 — Шаг 5: Extractor TAR→MP3 + AssemblyAI STT
- [x] 2026-03-17 — Шаг 6: LLM-анализ (`analyzer.py`) — встроены approved MVP-1 checklist, contract и prompt framework
- [x] 2026-03-18 — Финальная проверка Шага 6 на `TEST_CALL_TIMUR_2026-02-24_074137.json`: подтверждён approved contract path, проверка approved `stage_code`, валидация `criteria_results` и `LLMResponseError` для невалидных `stage_code` / `criterion_code`
- [x] 2026-03-18 — Шаг 6 фактически закрыт как `Analyzer Ready`: approved contract path подтверждён, AnalysisResult wrapper не менялся
- [x] 2026-03-18 — Подготовлен manual pilot execution mode: explicit whitelist/test target config, manual CLI/API trigger, persistence и test delivery notifications
- [x] 2026-03-18 — Delivery больше не placeholder: собрана single-call test notification для Telegram/Email без отправки реальным менеджерам
- [x] 2026-03-18 — Добавлен manual orchestration flow: OnlinePBX -> filtering -> extractor -> STT -> analyzer -> persistence -> delivery
- [x] 2026-03-18 — Шаг 7A.1a: исправлена нормализация OnlinePBX endpoint config, добавлены override env vars и диагностические ошибки для malformed URL / redirect / invalid endpoint
- [x] 2026-03-18 — Шаг 7A.1a: manual live STT path переведён на configurable Whisper provider через env
- [x] 2026-03-18 — Шаг 7A.1a: найден и подтверждён рабочий OnlinePBX HTTP API override `ONLINEPBX_CDR_URL=https://api.onlinepbx.ru/{domain}/mongo_history/search.json`, auth flow `auth.json -> x-pbx-authentication`, live `get_cdr_list()` возвращает реальные CDR записи без redirect
- [x] 2026-03-18 — Шаг 7A.1a: подтверждён live audio fetch на реальном кейсе `384e3a87-3d38-4a84-9656-0feec40be59a`, extractor теперь принимает прямой `rec.mp3`, а не только TAR-архив
- [x] 2026-03-18 — Шаг 7A.1a: добавлен минимальный diagnostic CLI `python -m app.agents.calls.onlinepbx_live_probe` и зафиксирован fresh-container runtime path через `docker compose run --rm api ...`
- [x] 2026-03-18 — Шаг 7A.1a: добавлен временный manual bootstrap для `department` / `manager` без Bitrix (`manual_pilot_bootstrap` + bootstrap args в `manual_runner`)
- [x] 2026-03-18 — Шаг 7A.1a: выполнен реальный manual live run на `external_id=384e3a87-3d38-4a84-9656-0feec40be59a` до transcript, analyzer и persistence; `interaction` и `analysis` созданы и связаны с pilot `department/manager`
- [x] 2026-03-18 — Шаг 7A.1a: test-only delivery path реально вызван на том же кейсе; текущий внешний блокер — `SMTPAuthenticationError 535`
- [x] 2026-03-18 — Шаг 7A.1a: `CallsAnalyzer` получил runtime-safe fallback для approved source materials внутри fresh container, а orchestrator теперь сохраняет `analyzed_at` даже при `DELIVERY_FAILED`
- [x] 2026-03-18 — Шаг 7A.1a: добавлен delivery-only replay для уже сохранённого `interaction` / `analysis` без повторного STT/LLM; replay подтверждён на кейсе `interaction_id=2ea673d2-8a5c-4ab3-9e96-339392003b00` без создания дублей
- [x] 2026-03-18 — Шаг 7A.1a: фактическая причина незавершённой test delivery подтверждена в runtime — в project `.env` отсутствуют `TEST_DELIVERY_EMAIL_TO` / `TEST_DELIVERY_TELEGRAM_CHAT_ID`, а `SMTP_USER`, `SMTP_PASSWORD`, `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID_ROP` остаются placeholder-like значениями; replay даёт `Email delivery failed: SMTP auth error 535`
- [x] 2026-03-18 — Шаг 7A.1a: Telegram test-only path дополнительно проверен — `TELEGRAM_BOT_TOKEN` валиден (`getMe -> ok=true`, bot `gmixam_test_bot`), но `TEST_DELIVERY_TELEGRAM_CHAT_ID` по-прежнему не задан, поэтому Telegram replay ещё нельзя завершить безопасно без явного test chat id
- [x] 2026-03-18 — Шаг 7A.1a: выполнен Telegram-focused replay на existing `interaction_id=2ea673d2-8a5c-4ab3-9e96-339392003b00`; дублей не создано, а текущая фактическая ошибка зафиксирована как `No test delivery target configured. Set TEST_DELIVERY_TELEGRAM_CHAT_ID and/or TEST_DELIVERY_EMAIL_TO.`
- [x] 2026-03-18 — Шаг 7A.1a: финальный Telegram delivery-only replay успешно выполнен на existing `interaction_id=2ea673d2-8a5c-4ab3-9e96-339392003b00` с `TEST_DELIVERY_TELEGRAM_CHAT_ID=74665909`; сообщение отправлено в test-only чат, `interaction.status=DELIVERED`, audit в metadata обновлён, дублей `interaction/analysis` нет

- [x] 2026-03-18 — Веха 3.5 `Manual Live Validation` формально закрыта: manual live pipeline подтверждён на реальном кейсе от OnlinePBX до persistence и test-only Telegram delivery output
- [x] 2026-03-18 — Добавлен минимальный Bitrix24 read-only integration path: masked client, deterministic mapping (`extension -> phone -> manual fallback`), on-demand mirror в существующие `departments/managers` и diagnostic CLI `python -m app.agents.calls.bitrix_readonly_probe`
- [x] 2026-03-18 — Совместимость manual fallback сохранена: при пустом/placeholder `BITRIX24_WEBHOOK_URL` read-only path не ломает intake и отдаёт понятную masked diagnostic ошибку
- [x] 2026-03-18 — Live Bitrix24 connectivity подтверждена на webhook `https://dogovor24.bitrix24.kz/rest/***/`: `user.get` и `department.get` работают в read-only режиме, `UF_PHONE_INNER` и `UF_DEPARTMENT` реально приходят в `user.get`
- [x] 2026-03-18 — Live manager mapping подтверждён на extension `311`: найден Bitrix user `2158` (`Тимур Жуматаев`), mapping source=`bitrix_extension`, локально создан/обновлён mirror manager `656abe58-7c23-476a-a9f6-d76305cf42e0` и department `472cda28-ce71-494c-9068-25d3ffbf7399` (`bitrix_department_id=7`)
- [x] 2026-03-18 — По live Bitrix24 найден и исправлен минимальный gap: поле `ACTIVE` в этом аккаунте приходит как boolean, поэтому нормализация read-only mapper теперь поддерживает и boolean, и legacy string формы без ломки fallback path
- [x] 2026-03-18 — Подготовлен fact-based inventory текущих output materials и создан `MANUAL_OUTPUT_VALIDATION_SPEC.md` для следующего ручного шага проверки качества
- [x] 2026-03-18 — Выполнен первый фактический verification run `MANUAL_OUTPUT_VALIDATION_RUN_001.md` на persisted live кейсе `interaction_id=2ea673d2-8a5c-4ab3-9e96-339392003b00`: transcript, analysis, persistence и delivery audit подтверждены одним evidence pack без нового live run
- [x] 2026-03-18 — В `MANUAL_OUTPUT_VALIDATION_RUN_001` найден high-severity gap: compact Telegram/manager-facing delivery formatter не совпадает с фактической структурой persisted `strengths/gaps/recommendations`, из-за чего в delivery появляются `None`-значения
- [x] 2026-03-18 — В `MANUAL_OUTPUT_VALIDATION_RUN_001` подтверждено, что full manager card по approved `MVP1_MANAGER_CARD_FORMAT_v1.md` всё ещё отсутствует как отдельный runtime artifact; сейчас реализован только compact single-call delivery summary
- [x] 2026-03-18 — В `MANUAL_OUTPUT_VALIDATION_RUN_002_DELIVERY_FIX.md` устранён blocking delivery defect: compact manager-facing card теперь поддерживает legacy persisted shape и не печатает `None / None`, `None: None` и `[medium] None -> None`
- [x] 2026-03-18 — Зафиксирована и внедрена runtime language policy: transcript/source fragments остаются на исходном языке, а business-facing delivery output рендерится на русском языке; analyzer prompt и validation spec синхронизированы с этим правилом
- [x] 2026-03-18 — Добавлены regression tests `tests/test_calls_delivery_text.py`: delivery render на legacy persisted shape, защита от unwanted transcript translation и analyzer-side shape normalization для будущих persisted outputs
- [x] 2026-03-18 — Выполнен финальный Telegram delivery-only replay на existing `interaction_id=2ea673d2-8a5c-4ab3-9e96-339392003b00` после delivery fix: `interaction.status=DELIVERED`, target `74665909`, дублей `interaction/analysis` нет, updated card preview зафиксирован в `MANUAL_OUTPUT_VALIDATION_RUN_002_DELIVERY_FIX.md`
- [x] 2026-03-19 — Реорганизован инструкционный слой для ИИ-кодера: постоянные project-wide rules вынесены в `docs/CODER_WORKING_RULES.md`, stage-specific правила очищены и оставлены в `docs/MANUAL_OUTPUT_VALIDATION_SPEC.md`, короткий шаблон будущих task-промптов добавлен в `docs/TASK_PROMPT_TEMPLATE.md`
- [x] 2026-03-19 — `docs/CONTEXT_INDEX.md` обновлён под более быстрый вход в задачу: новый порядок чтения начинается с coder working rules, а `docs/PROMPTS_GUIDE.md` теперь явно разделяет постоянные prompt policies и task-specific prompt instructions
- [x] 2026-03-19 — Для разблокировки `Run 003` через existing manual MVP-1 flow получен второй persisted analyzed case: `interaction_id=52fa496a-6045-4d0f-aba5-f5e98e3c2da8`, `analysis_id=096745aa-5c00-4213-b4d4-e1a252c1416e`, `external_id=fe449b87-6c82-48d0-89de-ef9a2fac3cdc`, итоговый `status=DELIVERED`
- [x] 2026-03-19 — Новый кейс для `Run 003` содержит transcript, persisted analysis, delivery audit и `agreements_count=1`, поэтому cross-case Manual Output Validation теперь операционно разблокирована
- [x] 2026-03-19 — Выполнен `Manual Output Validation Run 003` на втором persisted case `interaction_id=52fa496a-6045-4d0f-aba5-f5e98e3c2da8`: подтверждены transcript, analysis, delivery, agreements path, consistency и Russian business-facing output на втором реальном payload
- [x] 2026-03-19 — По итогам `Run 003` delivery fix из `Run 002` подтверждён уже на двух кейсах; language policy также подтверждена на двух кейсах
- [x] 2026-03-19 — Выполнен `Run 004` по manager mapping / manager identity для кейса `interaction_id=52fa496a-6045-4d0f-aba5-f5e98e3c2da8`: primary root cause подтверждён как `mapping_issue` (`manager_id=null`, `manual_fallback`, no local manager `322`, Bitrix read-only currently `configured=False` из-за placeholder webhook)
- [x] 2026-03-19 — Для `Run 004` сделан минимальный local render fix: compact delivery теперь должен показывать явный fallback label для unresolved manager identity вместо голой extension
- [x] 2026-03-19 — Follow-up verification для `Run 004` подтвердил, что current workspace `.env` и current `api` runtime env оба содержат placeholder `BITRIX24_WEBHOOK_URL`, поэтому live Bitrix24 probe для `322` в этой среде не воспроизводится; одновременно подтверждено, что persisted `analyses.scores_detail.call.manager_name=\"322\"` и delivery audit лишь унаследовали уже сохранённый fallback-state, а не создали новый rendering/persistence defect
- [x] 2026-03-19 — Дополнительная Track A verification уточнила code path для кейса `322`: при `settings.has_bitrix24_readonly=False` mapper возвращает `None`, после чего именно `CallsIntake.resolve_manager_mapping()` добавляет `no_local_or_bitrix_match`; это подтверждает skipped Bitrix lookup из-за config-state, а не live negative Bitrix match result по `322`
- [x] 2026-03-19 — После добавления real `BITRIX24_WEBHOOK_URL` и fresh runtime recreate live Bitrix probe для `322` успешно воспроизведён: найден user `1877` (`Эльмира Кешубаева`), `source=bitrix_extension`, локально создан mirror manager `09cae83f-7ac1-4ee0-b1d5-3a76c8053c3f`; это перевело Track A root cause из `not yet reproducible config-side issue` в `config issue resolved`, при том что уже persisted fallback-case не был backfilled автоматически
- [x] 2026-03-19 — Track A закрыт на bounded scope для кейса `322`: targeted re-intake обновил historical `interaction` до `manager_id=09cae83f-7ac1-4ee0-b1d5-3a76c8053c3f` и `mapping_source=bitrix_extension`; дополнительно найден и исправлен локальный persistence-sync defect для already existing analysis rows, после чего `analysis_id=096745aa-5c00-4213-b4d4-e1a252c1416e` и persisted `scores_detail.call` стали консистентны с resolved manager identity
- [x] 2026-03-19 — Manual Output Validation формально закрыт: manual pipeline, transcript/analysis consistency, delivery readability, language policy, Track B provider-routing bounded scope и Track A Bitrix manager mapping подтверждены на representative cases; blocking defects текущей фазы закрыты без перехода в automation readiness
- [x] 2026-03-26 — Project documentation переведена на новый согласованный промежуточный режим `Manual Reporting Pilot`: он зафиксирован как manual parameterized reporting step между `Delivery Ready` и future automation settings, с presets `manager_daily` / `rop_weekly`, email-only delivery, Bitrix-based recipient resolution, reuse-first recomputation rules и optional bounded report-composer direction
- [x] 2026-03-19 — Добавлен shared AI provider routing layer для `STT` / `LLM-1` / `LLM-2`: отдельные provider pools, account aliases, deterministic policies `fixed|failover|weighted_ab|manual_force`, legacy single-provider fallback и audit metadata persistence в `interaction.metadata.ai_routing`
- [x] 2026-03-19 — Реальная execution-интеграция routing layer подтверждена для `STT` и `LLM-2`; для `LLM-1` routing/config слой подготовлен и сохраняется в metadata как `executed=false`, без включения отдельного first-pass runtime call в текущем `CallsAnalyzer`
- [x] 2026-03-19 — Backward compatibility сохранена: legacy STT/manual-live path с provider override (`assemblyai` / `whisper`) продолжает работать без обязательного multi-provider JSON config; analyzer contract и manual MVP-1 flow не менялись
- [x] 2026-03-19 — Добавлены unit tests `tests/test_ai_provider_routing.py` на `fixed`, `failover`, `weighted_ab`, `manual_force`, single-provider compatibility и metadata persistence helper
- [x] 2026-03-26 — Реализован первый bounded Manual Reporting Pilot slice: отдельные manual CLI/API entrypoints для report run, preset resolution для `manager_daily` / `rop_weekly`, reuse-first selection по persisted `interactions` / `analyses`, bounded `build_missing` для missing transcript/analysis внутри selected persisted calls, normalized payload builders, email-first rendering и reporting email skeleton с `To + Cc + text/html`
- [x] 2026-03-26 — Для Manual Reporting Pilot введены structured statuses/errors (`no_data`, `missing_artifacts`, `recipient_blocked`, `ready`, `delivered`, `partial`, `blocked`) и minimal recipient resolution rules: `manager_daily -> managers.email`, `rop_weekly -> departments.settings.reporting.rop_weekly_email`, monitoring copy default `sales@dogovor24.kz`
- [x] 2026-03-26 — Добавлены unit tests `tests/test_manual_reporting.py` на preset resolution, normalized payload assembly, CRM placeholder preservation, rendered email preview, `missing_artifacts`, `recipient_blocked` и successful delivery-ready path; container verification подтверждена для `/app/tests/test_manual_reporting.py`
- [x] 2026-03-27 — Проведён real validation/hardening bounded Manual Reporting Pilot slice через текущие CLI/API path: `manager_daily` и `rop_weekly` на live persisted department `472cda28-ce71-494c-9068-25d3ffbf7399` возвращают `ready` в `report_from_ready_data_only` при `--no-delivery`, `build_missing_and_report` проходит тот же reuse-first path без rebuild, а `POST /pipeline/calls/report-run` отдаёт тот же structured result для weekly
- [x] 2026-03-27 — По real weekly validation найден и закрыт blocking recipient-resolution gap: если `departments.settings.reporting.rop_weekly_email` не задан, `rop_weekly` теперь использует fallback `Bitrix department.UF_HEAD -> active user EMAIL`, что сняло `recipient_blocked` с live weekly scenario без broad org-structure redesign
- [x] 2026-03-27 — По real delivery validation найден и закрыт blocking control-flow defect: email delivery failures в reporting path больше не роняют CLI/API stack trace, а возвращаются как structured report-level `blocked` с сохранёнными `payload` и `preview`
- [x] 2026-03-27 — Текущий внешний блокер фактического email `delivered` status подтверждён повторно уже после hardening: runtime доходит до SMTP login, но текущие credentials в env отвечают `Email delivery failed: SMTP auth error 535`; это больше не code-level crash, но остаётся operational limitation среды
- [x] 2026-03-27 — Повторная delivery-only validation после supposed SMTP update не подтвердила `delivered` happy path: и CLI, и API для `manager_daily`/`rop_weekly` возвращают structured `blocked`, direct runtime `smtp.login()` в `api` container по-прежнему падает с `SMTPAuthenticationError 535`, при этом recipient resolution подтверждён отдельно на live data (`manager_daily -> k.elmira@dogovor24.kz + sales@dogovor24.kz`, `rop_weekly -> b.urkenay@dogovor24.kz + sales@dogovor24.kz`)
- [x] 2026-03-27 — Operational SMTP blocker закрыт без кодового transport patch: после recreate `api` container с `smtp.mail.ru:587` direct `smtp.login()` прошёл успешно, а `manager_daily` и `rop_weekly` подтвердили фактический `delivered` и через CLI, и через API
- [x] 2026-03-27 — Реализован первый минимальный operator UI для Manual Reporting Pilot внутри текущего FastAPI app: отдельная внутренняя web-страница поверх existing reporting API path, form context по отделам/менеджерам, recipient preview и отображение run result (`status`, report status, blocking reason/error) без нового frontend-проекта и без смены `/pipeline/calls/report-run` contract
- [x] 2026-03-27 — Проведён usability-hardening первого operator UI: добавлен явный Bitrix-backed refresh local manager directory для выбранного отдела, selectors переведены на multi-select для `manager_ids[]` / `manager_extensions[]`, а operator delivery path получил Telegram-first test mode wrapper с сохранением resolved email recipients в preview/result и email fallback при отсутствии test chat id
- [x] 2026-03-27 — Live smoke для operator UI hardening подтверждён: `POST /pipeline/calls/report-ui/sync-managers` обновил `[ЭДО] Отдел Продаж` с 2 до 13 active mirrored managers, UI page отдаёт multi-select controls и sync action, а `POST /pipeline/calls/report-run` принимает multi-manager payload без смены execution contract; unit tests `11 passed` в `api` container
- [x] 2026-03-27 — Live Telegram happy path для operator run подтверждён после задания `TEST_DELIVERY_TELEGRAM_CHAT_ID` и recreate `api`: runtime `report-ui/context` показывает `operator_delivery.mode=telegram_test`, а `POST /pipeline/calls/report-run` для `manager_daily` на live department `472cda28-ce71-494c-9068-25d3ffbf7399` вернул `completed -> delivered` с фактическим target `telegram:74665909` и сохранённым `resolved_email={primary_email: k.elmira@dogovor24.kz, cc: [sales@dogovor24.kz]}` в response transport metadata
- [x] 2026-03-27 — Добавлен первый bounded observability UI step внутри existing Manual Reporting Pilot: `report-run` response теперь включает structured `observability` block с run-state, stage statuses (`selection`, `reuse-check`, `build-missing`, `report-build/render`, `delivery`), run summary и AI cost fallback; internal operator page показывает `starting/running` сразу на client side и затем отрисовывает backend observability без нового endpoint и без смены `/pipeline/calls/report-run` contract
- [x] 2026-03-27 — Runtime smoke для observability UI подтверждён: `POST /pipeline/calls/report-run` на live manager_daily case вернул `observability.summary.selected_interactions_count=1`, `reused_analyses_count=1`, `rebuilt_analyses_count=0`, `delivery.mode=telegram_test`, `delivery.result=skipped` при `send_email=false`; unit tests `13 passed` в `api` container
- [x] 2026-03-27 — Existing operator page усилен bounded diagnostics block без смены layout и execution contract: `report-run` теперь возвращает structured `diagnostics` c effective filters, selection transparency counts, local-mirror hints, stable reason codes и mode notes (`ready-only` vs `build-missing`), а UI показывает это на той же странице под existing Run Observability
- [x] 2026-03-27 — Runtime/API smoke для diagnostics подтверждён: live `manager_daily` run вернул `diagnostics.manager_filter_logic=intersection`, `interactions_found_before_reuse_build=1`, `ready_transcripts_count=1`, `ready_analyses_count=1`, `final_selected_interactions_count=1`, notes про `report_from_ready_data_only` и filters intersection; unit tests расширены до `15 passed`
- [x] 2026-03-27 — Manual Reporting operator path переопределён из persisted-reporting в source-aware full manual run: `/pipeline/calls/report-run` теперь сначала делает OnlinePBX source discovery по выбранному периоду и manager filters, затем выполняет persistence-check и idempotent ingest missing interactions, и только после этого переходит к reuse/build/report stages
- [x] 2026-03-27 — Закрыт live source-discovery blocker для operator full-run: default OnlinePBX CDR resolution переведён на working HTTP API path `https://api.onlinepbx.ru/{domain}/mongo_history/search.json` для current OnlinePBX host patterns, поэтому manual report-run больше не зависит от отдельного `ONLINEPBX_CDR_URL` override для этого runtime path
- [x] 2026-03-27 — Закрыты два blocking AI-routing compatibility gaps для full manual run: OpenAI-compatible `STT` executor и `LLM-2` executor теперь не блокируются known-safe endpoint hints (`/audio/transcriptions`, `/chat/completions`) из provider-pool config, а operator run больше не ломается на artificial adapter mismatch
- [x] 2026-03-27 — Source/build failures в manual full-run path нормализованы обратно в structured `blocked` / `partial` reporting result с stage-level observability errors вместо raw HTTP 400 traceback-only failure, что сохраняет полезность operator UI даже при runtime проблемах source/STT/analysis
- [x] 2026-03-27 — Live bounded validation нового operating model подтверждена на `manager_daily` для department `472cda28-ce71-494c-9068-25d3ffbf7399`, manager `322`: `report_from_ready_data_only` нашёл 4 matched source calls, ingest-нул 3 missing interactions и вернул structured `completed`; затем `build_missing_and_report` reuse-first достроил 3 missing analyses и завершился `completed` с `interaction_count=4`, `analysis_count=4`, full observability chain и без дублей
- [x] 2026-03-27 — Delivery semantics для operator run разведена по каналам: каждый manual run теперь всегда пытается отправить итоговый report в `TEST_DELIVERY_TELEGRAM_CHAT_ID`, а UI toggle управляет только business email delivery и по умолчанию выключен; live API smoke подтвердил это и для `manager_daily`, и для `rop_weekly` (`telegram_test_delivery=delivered`, `email_delivery=skipped`, overall report status=`delivered` при `send_email=false`)
- [x] 2026-03-30 — Первая стандартная template version подключена в manual reporting: active assets переведены на `manager_daily_template_v1` и `rop_weekly_template_v1`, новые visual reference files `docs/report_templates/reference/manager_daily_reference.md` и `docs/report_templates/reference/rop_weekly_reference.md` зафиксированы как layout source of truth, а renderer/template layer обновлён под standard v1 naming и более близкий reference layout
- [x] 2026-03-30 — Live artifact smoke подтверждён на обоих preset’ах через existing `/pipeline/calls/report-run`: `manager_daily` и `rop_weekly` возвращают `artifact{kind=pdf_report, template_version=manager_daily_template_v1|rop_weekly_template_v1, page_count=3}`, observability summary показывает exact active template version, а always-on Telegram test delivery уходит как `.pdf` document (`manager_daily_*_manager_daily_template_v1.pdf`, `rop_weekly_*_rop_weekly_template_v1.pdf`) при `send_email=false`
- [x] 2026-03-30 — `manager_daily_template_v1` доведён до runtime adaptation от approved HTML reference: `manager_daily` render path теперь использует reference-aligned CSS/HTML composition, cleaner editorial fallbacks и убирает reader-facing service traces (`Note:`, raw `not available`, template/debug lines) из final PDF/preview artifact без смены existing manual run contract
- [x] 2026-03-30 — Выполнен bounded corrective visual polish для `manager_daily_template_v1`: PDF path для daily report переведён с text-flow rendering на fixed reference-like composition по страницам (hero, metric tiles, summary box, banners, review grid, problem card, recommendation cards, outcomes table, dynamics block, memo page), fallback states стали manager-facing и artifact live smoke теперь возвращает `page_count=4` без поломки Telegram PDF delivery
- [x] 2026-03-27 — Execution model разведён по preset’ам как standing runtime rule: `manager_daily` сохраняет source-aware manual full-run behavior, а `rop_weekly` переведён на hard persisted-only aggregation без source discovery, ingest missing calls и без нового audio/STT/analysis execution
- [x] 2026-04-07 — Для `manager_daily` добавлен bounded readiness decision layer поверх Manual Reporting Pilot slice: daily path больше не обязан всегда выпускать full report; после source discovery / ingest / reuse / optional build и до render/delivery runtime теперь выбирает `full_report`, `signal_report` или `skip_accumulate`
- [x] 2026-04-07 — Зафиксированы стартовые thresholds readiness policy для `manager_daily`: `full_report` требует `relevant_calls >= 6`, `ready_analyses >= 5`, `analysis_coverage >= 75%` и content-readiness key blocks без empty fallback; `signal_report` требует `ready_analyses >= 2` плюс explicit signal и manager action
- [x] 2026-04-07 — Rolling window для readiness decision ограничен `1 -> 2 -> 3` рабочими днями; structured run result теперь показывает `readiness_outcome`, `readiness_reason_codes`, `window_days_used`, `relevant_calls`, `ready_analyses`, `analysis_coverage` и presence/absence content blocks; `rop_weekly` оставлен без изменений
- [x] 2026-04-07 — В reporting path введены stricter reuse/version checks: transcript reuse по-прежнему требует non-empty `interactions.text`, а analysis reuse теперь требует reporting-compatible persisted shape (`instruction_version`, `classification`, `score.checklist_score.score_percent`, `score_by_stage`, `strengths`, `gaps`, `recommendations`, `follow_up`); stale/incomplete analysis больше не считается ready for reuse
- [x] 2026-04-07 — В `manager_daily/build_missing_and_report` stale/non-reusable analysis теперь пересобирается как bounded analysis-only rebuild при already-ready transcript, а в `report_from_ready_data_only` такая analysis остаётся вне ready subset и даёт structured reuse-rejection diagnostics вместо молчаливого reuse
- [x] 2026-04-07 — Payload richness усилен без смены normalized contract: `manager_daily` теперь богаче использует `score_by_stage`, `follow_up`, `product_signals`, `evidence_fragments` для narrative/focus/problem/outcomes/dynamics, а `rop_weekly` — для более содержательных `dashboard_rows`, `risk_zone_cards`, `systemic_team_problems`, `rop_tasks_next_week` и commentary внутри weekly snapshot
- [x] 2026-04-07 — Активирован реальный separate `LLM-1` runtime pass в `CallsAnalyzer`: fresh/missing `manager_daily/build_missing_and_report` cases теперь проходят полный upstream chain `audio fetch -> STT -> LLM-1 -> LLM-2 -> persistence -> report/delivery`, при этом approved analyzer contract остаётся финальным output только от `LLM-2`
- [x] 2026-04-07 — Для AI layers усилен audit trail: `interaction.metadata.ai_routing` теперь хранит `selected_api_key_env`, `execution_status`, `skip_reason`, `request_kind`, `usage`, `attempted_count`, а operator reporting observability показывает per-layer `STT | LLM-1 | LLM-2` status как `executed | reused_only | skipped`
- [x] 2026-04-07 — Закрыт analyzer semantic-validation gap: shape-valid, но semantically empty `LLM-2` output теперь не считается successful analysis; после shape-validation добавлен bounded semantic check, canonical reason code=`semantically_empty_analysis`, а такой output сохраняется только как failed analysis attempt вместо normal reusable analysis
- [x] 2026-04-07 — Закрыт forensic gap между raw и normalized analyzer result: `analyses.raw_llm_response` теперь хранит raw `LLM-2` response text, тогда как normalized approved-contract snapshot остаётся отдельным persisted result и при semantic-invalid case помечается только как failed forensic snapshot
- [x] 2026-04-07 — Reporting reuse safety net усилен против semantic-empty analyses: failed rows с `semantically_empty_analysis` и старые persisted semantic-empty contracts больше не считаются reusable; в `manager_daily/build_missing_and_report` при ready transcript пересобирается только analysis step, а в `report_from_ready_data_only` interaction остаётся вне ready subset с structured reuse-rejection reason
- [x] 2026-04-08 — Выполнена bounded verification `STT/OpenAI wiring`: кодовый path и persisted runtime metadata подтверждают `AI_STT_ROUTING_POLICY=fixed`, `account_alias=stt_main`, `api_key_env=OPENAI_API_KEY_STT_MAIN`, `provider=openai`, `model=whisper-1`, `api_base=https://api.openai.com/v1`; дополнительно добавлены minimal STT audit fields `executed_endpoint_path` и `provider_request_id` для прямой сверки route-audit с actual SDK execution site без утечки секрета
- [x] 2026-04-08 — Закрыт environment-side verification blocker: fresh one-off `api` container в normal project environment успешно выполнил targeted suite `python -m unittest tests.test_ai_provider_routing tests.test_manual_reporting` (`46 tests OK`), а найденные compatibility-regressions были bounded и локальны: test settings isolation against `.env` fixed aliases, safe fallback for lightweight reporting test doubles (`is_failed/fail_reason`, missing `db/department_id`) и synced `manual_reporting_reuse_v3` expectation
- [x] 2026-04-08 — Fresh runtime verification подтверждён на existing CLI/API/operator paths без опоры на stale long-running `api`: `GET /pipeline/calls/report-ui` и `GET /pipeline/calls/report-ui/context` отдают `200` в fresh `TestClient`; `manager_daily/report_from_ready_data_only` на live case `department=472cda28-ce71-494c-9068-25d3ffbf7399`, `manager=09cae83f-7ac1-4ee0-b1d5-3a76c8053c3f`, `extension=322`, `period=2026-04-03` подтвердил readiness logic, rolling window `1->3`, stricter reuse rejection of `semantically_empty_analysis` and no new `audio/STT/LLM-1/LLM-2` builds
- [x] 2026-04-08 — Fresh `manager_daily/build_missing_and_report` verification на live case `period=2026-04-06`, same manager/extension, подтвердил source-aware manual path, analysis-only rebuild for transcript-ready interactions, actual `audio fetch -> STT -> LLM-1` entry on missing/fresh interactions и current AI-layer audit wiring; residual blocker for full e2e confirmation now narrowed to operational quota exhaustion (`STT=429 insufficient_quota`, `LLM-1=429 insufficient_quota`), not to reporting/analyzer/runtime wiring defects
- [x] 2026-04-08 — Final verification-closure rerun подтвердил, что OpenAI runtime keys снова отвечают (`/v1/models -> 200` для `OPENAI_API_KEY_STT_MAIN` и `OPENAI_API_KEY_LLM1_MAIN`), а actual HTTPS manual trigger path через `/pipeline/calls/report-run` остаётся рабочим: live `rop_weekly` rerun по `2026-04-02..2026-04-06` реально отдал `reports[0].status=delivered` и доставил PDF в `telegram_test_delivery.target=74665909` при `send_email=false`
- [x] 2026-04-08 — Несмотря на восстановленный access-level healthcheck, final fresh/missing `manager_daily` rerun по live case `period=2026-04-06`, `manager=09cae83f-7ac1-4ee0-b1d5-3a76c8053c3f`, `extension=322` всё ещё не закрыл последний full-chain gap: source discovery, ingest/reuse decision, analysis-only rebuild, audio fetch и STT entry подтвердились, но billable runtime calls снова упёрлись в confirmed `429 insufficient_quota` на `STT` и `LLM-1`, поэтому final status остаётся `gap partially closed` и снять `closed_with_known_verification_gap` пока нельзя по operational, а не code-level причине
- [x] 2026-04-08 — Final verification closure временно отложен осознанно: confirmed residual blocker остаётся внешним operational issue (`OPENAI_API_KEY_STT_MAIN` и `OPENAI_API_KEY_LLM1_MAIN` продолжают возвращать billable `429 insufficient_quota`), поэтому следующим bounded infra step выбран Git setup без новых code changes по reporting/analyzer pipeline и без маскировки открытого verification gap
- [x] 2026-04-09 — Для non-deliverable `manager_daily` добавлен operator-facing empty-state shell: при `skip_accumulate`, `no_data` или `missing_artifacts` reporting layer теперь рендерит preview-only PDF с layout daily report, diagnostics, readiness reason codes, counts/coverage и placeholder-секциями, явно маркирует artifact как `preview / insufficient data / not a deliverable manager report`, отключает business email и использует только test delivery path
- [x] 2026-04-09 — Live actual operator rerun подтверждён для ready-only case `department=472cda28-ce71-494c-9068-25d3ffbf7399`, `manager=09cae83f-7ac1-4ee0-b1d5-3a76c8053c3f`, `extension=322`, requested period `2026-04-06`, effective window `2026-04-02..2026-04-06`: outcome остался `skip_accumulate`, но runtime собрал `manager_daily_preview_Эльмира Кешубаева_2026-04-02_manager_daily_template_v1.pdf` (`page_count=4`) и доставил его в `telegram_test_delivery.target=74665909` при `send_email=false`, без запуска новых `STT / LLM-1 / LLM-2`
- [x] 2026-04-09 — Agent-independent process hardening layer зафиксирован в repo: `CLAUDE.md` переписан как тонкий entry adapter → `docs/CODER_WORKING_RULES.md`; добавлены `AGENTS.md` и `GEMINI.md` как entry adapters для OpenAI Codex и Gemini; `docs/CODER_WORKING_RULES.md` расширен правилами 0 (repo-first source of truth), 4 (task normalization rule), 8 (close-out rule) и нумерация разделов выровнена; `docs/TASK_PROMPT_TEMPLATE.md` обновлён под новую структуру с обязательным close-out чеклистом; `docs/DECISIONS.md` пополнен ADR-041 (repo-first governance + agent-independent task closure) и ADR-042 (thin entry adapters + versioned git hook barrier)
- [x] 2026-04-09 — Добавлен versioned git hook infrastructure: `scripts/hooks/pre-push-check.sh` (логика барьера), `scripts/hooks/pre-push` (тонкий wrapper), `scripts/install-git-hooks.sh` (установщик); pre-push hook блокирует push если есть non-doc изменения без обновления `docs/PROGRESS.md`, override возможен только через `git push --no-verify`; `.git/hooks/pre-push` устанавливается как symlink на repo-versioned script, что делает барьер agent-independent и versioned
- [x] 2026-04-09 — `manual_reporting_operator.html` переработан в summary-first layout: добавлен `#op-summary` с 4 плитками (статус / артефакт / доставка / AI cost) и строкой рекомендованного действия; форма сжата, вторичные контролы убраны в `<details>`; правая колонка заменена на per-state artifact panel (`delivered` / `skip_accumulate` / `no_data` / `missing_artifacts` / `blocked`); добавлена новая секция `#manager-readiness` из `diagnostics.readiness.groups[]` (только для `manager_daily`); observability и diagnostics свёрнуты в `<details>` по умолчанию; добавлены Russian label tables (16 reason codes, STATUS_RU, READINESS_RU); rop_weekly badge "persisted-only" добавлен к preset selector; skipped stages для rop_weekly маркированы как "норма (persisted-only)"; при transport/request failure observability `<details>` автоматически раскрывается; backend не трогался
- [x] 2026-03-27 — Live weekly smoke подтвердил новое preset-specific behavior: `rop_weekly` через `POST /pipeline/calls/report-run` возвращает `execution_model=persisted_only`, source/build stages приходят как `skipped`, `reused_analyses_count=4`, `rebuilt_analyses_count=0`, а weekly report остаётся ready/completed без открытия нового source/build chain
- [x] 2026-03-27 — Выполнен bounded error-envelope hardening для operator run: `/pipeline/calls/report-run` теперь возвращает JSON error envelope и на expected `ASAError`, и на unexpected server failure внутри manual reporting flow, при этом success contract не менялся
- [x] 2026-03-27 — `manual_reporting_operator.html` больше не делает unsafe `response.json()` на любой failed response: UI сначала читает status/content-type/raw body, различает request/transport failure и business-stage failure, и показывает readable request failure block вместо parse error уровня `Unexpected token 'I'`
- [x] 2026-03-27 — Regression tests расширены до `21 passed`: добавлены API-level проверки на `400 JSON envelope` и `500 JSON envelope` для `/pipeline/calls/report-run`

## В работе сейчас
- [ ] Веха 6.5 `Business-ready Report Pack` — реализация Task 1–6 из `docs/BUSINESS_READY_REPORT_PACK_TASKS.md`
- [ ] Внешний blocker по billable access для `OPENAI_API_KEY_STT_MAIN` и `OPENAI_API_KEY_LLM1_MAIN` остаётся open; closure rerun выполнить после user-confirmed top-up
- [ ] Full Report Mechanism Upgrade (WP1–WP7) отложен на отдельный post-pilot step; source of truth — `docs/RICH_DAILY_AND_PILOT_CHANGESET_PLAN.md`

## Следующие шаги
1. Реализовать Task 1–6 из `docs/BUSINESS_READY_REPORT_PACK_TASKS.md` (корзина «Делать сейчас»)
2. Live verification финального PDF и Telegram delivery после реализации
3. После верификации перейти к задачам второй корзины из того же документа
4. Провести `Pilot Live` на стабильной версии после `Business-ready Report Pack`
5. Выполнить bounded closure rerun `manager_daily/build_missing_and_report` после user-confirmed top-up (независимо от текущего шага)

## Открытые вопросы
- Чек-лист оценки звонков от РОПа (нужен до Шага 5)
- Для текущего OnlinePBX аккаунта legacy endpoint после host normalization по-прежнему отвечает `307 redirect` на `https://onlinepbx.ru/`; рабочий live path теперь подтверждён только через explicit `ONLINEPBX_CDR_URL` override на HTTP API
- Long-running `api/worker` контейнеры могут жить со stale env; для live validation подтверждён и задокументирован fresh-container путь `docker compose run --rm api ...`
- Временный bootstrap `department/manager` уже добавлен и работает, но это только переходный путь до будущего Bitrix24 read-only mapping
- Временные manual pilot bootstrap и delivery replay остаются переходным operational path и fallback-режимом даже после добавления Bitrix24 read-only path
- `user.userfield.list` для текущего webhook отвечает `insufficient_scope`, но это не блокирует mapping, потому что `UF_PHONE_INNER` и `UF_DEPARTMENT` уже присутствуют в `user.get`
- Полноценная manager card по approved format пока ещё не генерируется как отдельный artifact: сейчас есть compact single-call card в delivery, а full manager-card format остаётся approved source asset для следующего шага
- Первый Manual Output Validation blocking issue по compact delivery formatter закрыт, но broader output validation продолжается: нужно отдельно валидировать остальные artifacts на новых кейсах и не считать full manager card реализованной
- На 2026-03-19 initial blocker для `Run 003` был в sampling gap, но затем был снят получением второго persisted analyzed case; после этого `Run 003` дал two-case подтверждение delivery fix и language policy
- На 2026-03-19 `Run 003` это two-case подтверждение дал, но выявил residual `mapping_issue`: для кейса `interaction_id=52fa496a-6045-4d0f-aba5-f5e98e3c2da8` manager resolution остался в `manual_fallback`, и compact delivery показывает `Менеджер: 322`
- На 2026-03-19 `Run 004` показал, что residual mapping issue — это не corruption delivery payload, а результат current runtime/config state: local mirror не знает extension `322`, а Bitrix read-only path в текущем `.env` фактически выключен placeholder webhook
- На 2026-03-19 follow-up к `Run 004` подтвердил ещё жёстче, что placeholder-state есть не только в `.env`, но и внутри current `api` container env; следовательно, без отдельного operational webhook input нельзя фактически проверить, должен ли `322` матчиться в live Bitrix24 или нет
- На 2026-04-07 отдельный `LLM-1` runtime first-pass уже активирован для fresh/missing `manager_daily/build_missing_and_report`, но это всё ещё bounded analyzer evolution без перехода к scheduler/retries/beat/automation scope
- На 2026-03-19 отдельно подтверждено, что docs и code по AI routing в целом согласованы: shared router реально wired для `STT` и `LLM-2`, `LLM-1` persisted как `executed=false`, а текущие bounded gaps — это adapter scope (`assemblyai` / `openai-whisper` для STT, OpenAI-compatible path для LLM) и пока неиспользуемое в executor path поле `endpoint`
- На 2026-03-19 подготовлен следующий bounded step для provider adapter readiness: routing-valid config теперь явно отделён от execution-ready capability; рекомендован combined guardrail с final hard stop на executor-preflight, без активации отдельного `LLM-1` pass и без broad refactor
- На 2026-03-19 этот bounded step реализован: добавлена shared capability map для execution compatibility, `STT` и `LLM-2` теперь делают explicit executor-preflight hard stop, а compatibility failure reason сохраняется в существующие `attempted[]` / `provider_failure` metadata без ломки legacy single-provider и manual STT override path
- На 2026-03-19 post-implementation validation для Track B подтверждена на 4 representative scenarios и 2 backward-compatibility cases: supported `STT` и `LLM-2` candidates проходят preflight, unsupported/incompatible cases fail-fast с ожидаемым audit trail, отклонений от docs не найдено
- На 2026-03-19 финальная de-legacy проверка в fresh runtime подтвердила: `OPENAI_MODEL_CLASSIFY` и `OPENAI_MODEL_ANALYZE` больше не влияют на routed model selection при явном `AI_LLM1/AI_LLM2_PROVIDERS_JSON`, но `OPENAI_API_KEY` пока нельзя удалять полностью из env из-за bootstrap-required поля в `Settings`; дополнительно подтверждено, что stale `AI_*_FIXED_ACCOUNT_ALIAS` остаются operational tail и должны чиститься вместе с pool config
- На 2026-03-19 formal closure Manual Output Validation отделил closed scope от non-blocking tails: full manager-card implementation, current worker issue, future `LLM-1` first-pass activation и возможная automation readiness preparation остаются отдельными next-phase вопросами и не блокируют завершение этой фазы
- На 2026-03-26 следующим согласованным шагом после закрытия Manual Output Validation зафиксирован `Manual Reporting Pilot`, а не automation readiness: ручной параметрический запуск отчётов, reuse existing artifacts, email-only delivery и bounded model/report-composer experiments теперь считаются ближайшим operating direction проекта
- На 2026-03-27 persisted-only assumption для operator reporting path больше не актуальна: current bounded slice уже source-aware, но `rop_weekly` CRM block по-прежнему остаётся placeholder, а report-composer пока оставлен как metadata slot + deterministic fallback
- На 2026-03-27 source-aware assumption теперь применима только к `manager_daily`; для `rop_weekly` standing rule уже другой: weekly path persisted-only by design
- На 2026-03-27 minimal weekly recipient fallback через live Bitrix `UF_HEAD` уже включён как standing runtime path, но richer org-structure logic beyond department-head resolution по-прежнему остаётся out of scope для текущего bounded slice

## Стек решений
| Компонент | Решение | Статус |
|---|---|---|
| Оркестратор | Python + Celery | ✅ Решено |
| БД | PostgreSQL | ✅ Решено |
| STT | AssemblyAI | ✅ Решено |
| LLM | GPT-4o / GPT-4o-mini | ✅ Решено |
| Хостинг | Self-hosted VPS | ✅ Решено |

## 🔧 Решённые проблемы
**Дата:** 2026-03-17
**Проблема:** Alembic требует шаблон `script.py.mako` в папке `migrations`
**Решение:** Создан стандартный `script.py.mako`

**Дата:** 2026-03-17
**Проблема:** Для Шага 6 отсутствуют чек-листы, карточки звонков и утверждённый контракт анализа
**Решение:** Подготовлен только технический scaffold analyzer/prompt framework без выдумывания бизнес-логики

**Дата:** 2026-03-17
**Проблема:** Нужно было встроить approved checklist/contract без потери criterion-level detail и без нарушения ADR-004
**Решение:** CallsAnalyzer переведён на approved MVP-1 checklist/contract framework с contract validation и prompt loading через `_get_prompt()`

**Дата:** 2026-03-18
**Проблема:** Контракт-валидация резала невалидные `stage_code` / `criterion_code`, но не проверяла полноту обязательной структуры `criteria_results`
**Решение:** В `analyzer.py` добавлена строгая проверка обязательных полей stage/criterion и соответствия approved `stage_name` / `criterion_name` с ошибкой `LLMResponseError` при нарушении

**Дата:** 2026-03-18
**Проблема:** Для первого live smoke нужен был полный e2e pipeline раньше, чем появятся Bitrix read-only, scheduler и retries
**Решение:** Введён временный manual pilot mode: явный target whitelist, ручной trigger, test-only delivery и persistence полного результата без подключения Bitrix и без scheduler

**Дата:** 2026-03-18
**Проблема:** `ONLINEPBX_DOMAIN` может приходить как полный host, из-за чего старая сборка URL ломала CDR intake, а после нормализации текущий endpoint всё ещё отвечает redirect вместо JSON
**Решение:** Добавлены `ONLINEPBX_API_BASE_URL` / `ONLINEPBX_CDR_URL`, безопасная диагностика assembled URL и redirect, чтобы быстрее найти рабочий live endpoint без утечки секретов

**Дата:** 2026-03-18
**Проблема:** Для test live run нельзя зависеть от AssemblyAI
**Решение:** Добавлен configurable STT provider и временный manual-live switch на Whisper через OpenAI

**Дата:** 2026-03-18
**Проблема:** Для реального аккаунта legacy CDR path-with-key не работает, а `docker compose exec` в уже поднятый API контейнер даёт ложную картину из-за stale env
**Решение:** Подтверждён working override `ONLINEPBX_CDR_URL=https://api.onlinepbx.ru/{domain}/mongo_history/search.json`, введён минимальный probe CLI и зафиксирован fresh-container runtime path через `docker compose run --rm api ...`

**Дата:** 2026-03-18
**Проблема:** Первый real manual run был заблокирован отсутствием `departments` / `managers`, а после этого fresh-container analyzer не видел `docs/mvp1_sources` по host-absolute path
**Решение:** Добавлен минимальный bootstrap `department/manager` для Manual Live Validation и runtime-safe fallback approved analyzer sources внутри `CallsAnalyzer`

**Дата:** 2026-03-18
**Проблема:** После успешных transcript, analyzer и persistence test-only delivery упирается во внешнюю SMTP auth ошибку
**Решение:** Delivery path подтверждён до реального SMTP login; добавлен delivery-only replay по уже сохранённому `interaction` / `analysis`, чтобы повторять только стадию доставки без дублей. Следующий шаг — задать валидные test credentials или test Telegram target и повторить replay
